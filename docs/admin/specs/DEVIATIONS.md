# ADM — deviations

One dated line per divergence from spec. A stranger should reconstruct what
happened in ten seconds. Append-only.

Format: `YYYY-MM-DD · TICKET · what the spec said → what shipped · why`

---

2026-09-01 · ADM-1 · spec said "reuse the vendored sheet" → the sheet was extended with a `side: "left" | "right"` prop, a `SheetTrigger` export, and two `sheet-slide-*-left` keyframes in `globals.css` · a caller cannot override the baked-in `right-0` from outside — two classes of equal specificity in one rule, decided by Tailwind's emit order. The file's own note asks to be extended when a second use case arrives. The existing lead drawer defaults to `right` and is unchanged.

2026-09-01 · ADM-1 · spec's Acceptance 3 originally shipped `Questions` as a live link to a route ADM-2 builds → it ships as a dimmed label instead · D-ADM-7 reinstated the `ready: false` pattern for Finances, and once it exists the flagged "link to a 404" concern has a better answer. ADM-2 flips one boolean.

2026-09-01 · ADM-1 · the rail's `<nav>` is `overflow-y-auto` expanded but `overflow-visible` collapsed · the scroller clipped the collapsed section flyouts, which are the only thing the collapsed rail exists to show. Collapsed the rail is four icons and has nothing to scroll. Anything added to the collapsed rail must respect that it cannot scroll.

2026-09-01 · ADM-1 · `specs/README.md` said "no new dependency" → `lucide-react@0.553.0` added, pinned, matching CC's version · Taylor ratified icons in the rail (D-ADM-1) and the non-negotiable was amended in the same pass. Icons are `/admin`-only; the public site is untouched.

2026-09-01 · ADM-2 · spec strongly preferred "an early return to a sibling hook" for the preview seam → shipped as a single `io` object (`LIVE_IO` / `PREVIEW_IO`) selected on one line · an early return would call a different set of hooks on the same component, which the rules of hooks forbid and `eslint-plugin-react-hooks` rejects; suppressing that lint to get a prettier seam would trade a real guarantee for a cosmetic one. The property the spec wanted is preserved and arguably stronger: the hook body no longer names `saveStep`, `readLocal`, `writeLocal` or `clearLocal` at all, so a future write has to reach through `io`.

2026-09-01 · ADM-2 · spec said the previewing hook should pin `state` to `idle` → `attempt()` returns early on `preview` to achieve that · noted in the code as cosmetic rather than load-bearing: `PREVIEW_IO` already makes every line below it a no-op. The early return only stops the save indicator announcing a save that did not happen.

2026-09-01 · ADM-2 · Acceptance 6 asked for a real engagement autosaving end to end → verified as far as the seam, not to the database · no Supabase credentials are available in this environment. What was run: the same step component, same script, with and without the provider. Live (no provider) fired `POST /admin/seamcheck?live=1 → 200` (the `saveStep` server action) and wrote `ta-intake:seamchec:business` with the typed values. Preview fired no POST and wrote nothing. Persistence past the action boundary is unchanged code and was not exercised.

2026-09-01 · ADM-2 · `StepHeading` is not reused in the stack · it is an `h1` that focuses itself on mount; nine of them would fight for focus and break the heading order Acceptance 11 requires. The preview writes its own `h2` in the same visual register, as the spec's technical notes anticipated.

2026-09-01 · post-ADM-2 (Taylor's review) · preview track labels "Website build" / "Portfolio build" → "Platform / durable" and "Coded / showcase" · both names are load-bearing in an admin tool and the coded track's internal key (`showcase`) is the one that catches people out. The client-facing `eyebrowFor` strings are approved copy and were deliberately NOT changed.

2026-09-01 · coded intake Step 1 · `leadRole` free text → single select over the roles entered above · a stored lead role absent from the typed roles stays on the option list, so editing a role's spelling cannot silently drop the answer. No schema change: `ChoiceAnswer` without `multiple` already stores a plain string.

2026-09-01 · coded intake Step 1 · added a live title preview under the roles block · ordering the line by lead role is the only place that answer has a visible consequence during intake. It is explicitly not a mockup of the site's type — the site does not exist yet.

2026-09-01 · coded intake Step 1 · `unions` → `credentials`, plus a new `affiliations` field · `unions` is retained in `showcase-intake.ts` and `showcase-answer-labels.ts` as deprecated and read-only. Dropping the key would silently delete the answer of any engagement that filled it before today. No input writes it any more.

2026-09-01 · coded intake Step 1 · `docs/websites/portfolio-intake-questions-v2.md` amended in place with dated amendment notes · that file declares itself the verbatim source for every showcase string, so changing the components without changing it would have had the next session "restore" the film-biased copy.

2026-09-01 · ADM-2 amended (Taylor) · the preview showed only the nine questionnaire steps → it now shows the whole flow in labelled bands: start form, deposit and add-ons, welcome, then the nine steps · the start form asks real questions before payment and the pay screen carries the upsell; reviewing the steps alone hid both, and hid the payment boundary.

2026-09-01 · ADM-2 amended (Taylor) · the non-negotiable "this page issues no query" is lifted for the product catalogue only · the pay section reads the real catalogue so it quotes what a client would be quoted. Inventing a price on a money screen was the alternative and was refused. The read is Taylor's own catalogue, never client data; a failed read renders an explanation rather than an empty section, so a missing pay screen can never read as "there is no payment step".

2026-09-01 · ADM-2 amended · the preview write-path audit grows from three callers to eight · added: `start-form`, `showcase-start-form` (minting an engagement), `pay-button`, `showcase-pay-button` (opening Stripe Checkout), `deposit-checkout`, `showcase-checkout` (promo validation). The two pay buttons are the single choke point for starting a Stripe session and are gated there.

2026-09-01 · coded intake start form · placeholder "Director and camera operator in Vancouver" → "Founder and product designer in Vancouver" · it sits on the public page before payment and was the first thing every non-film client read. `portfolio-intake-questions-v2.md` amended with it.

2026-09-01 · ADM-2 · heading outline in the stacked preview is flat, not nested: page `h1`, flow section `h2`, step `h2` · step components carry their own `h2` sub-headings and those are client-facing. Demoting step headings to `h3` would put an `h2` inside an `h3`, which is an inversion and worse than flatness. No level is skipped.

2026-09-01 · coded intake (Taylor's second review) · site kinds lose "(coming soon)" and `disabled`; "Something else" added with a conditional `siteKindsOther` input · new field plumbed through `startShowcaseIntakeInput`, `stepAboutSchema`, the start action, and the label map together, per the validator file's own law.

2026-09-01 · coded intake Step 4 · "Something else" added to how-the-work-is-organised; the pre-existing always-visible `organizationOther` ("Anything else?") is now conditional on it and relabelled "Tell us how" · a permanently open vague box under concrete choices reads as a second question. The field key is unchanged, so existing answers are unaffected.

2026-09-01 · coded intake · "Portfolio" removed from every string shared across categories — step 2/5/6 intros, step 7 logo help, step 8 contact help, page and layout titles · the coded track also serves consultants, speakers, and studios. The site-kind option labelled "Portfolio" is deliberately kept: there it names one category of five.

2026-09-01 · coded intake · `eyebrowFor` "Agora · Portfolio build" → "Agora · Custom build" · **[PROVISIONAL — Taylor]**. It appeared on every screen of the coded track including the pay and welcome screens a consultant sees. "Custom" is category-neutral; "Coded" is the public slug but reads as jargon to a client. One string to overrule.

2026-09-01 · PORT-10 scoped, not built · the business-primer AI touchpoint is written up as `docs/websites/specs/PORT-10-business-primer.md` for execution in a fresh thread, per Taylor. It reuses PORT-6's extraction service rather than forking it, and it will need adding to ADM-2's preview-gating audit list as a fourth step-body network caller.

2026-09-01 · ADM-2 · the catalogue `catch` swallowed the thrown error and substituted a generic sentence → it now prints the underlying message verbatim · `getBuildProduct` raises the exact remedy ("Run `yarn stripe:catalogue --apply` and `yarn db:seed`…") and discarding it left a reviewer with no cause and no fix. This screen is Taylor-only; there is no reader to protect from an error string.

2026-09-01 · catalogue · `seed-products.ts` overwrote `stripeProductId`/`stripePriceId` whenever the committed `STRIPE_CATALOGUE_IDS` map held a value, despite a comment claiming re-seeding must never wipe what `--apply` wrote → both now use `coalesce(existing, bootstrap)` · found via a dry run: `--apply` matches prices by nickname, five sandbox prices have drifted nicknames and would be archived and re-minted, and one of them (`price_1U6yhBRvld9FSVemUheh9bQU`, `stripe_setup`) is hardcoded in the map. A re-seed after an apply would have written that archived id back over the working one and broken that product's checkout. Committed map is now a bootstrap only; Stripe is the source of truth for ids.

2026-09-01 · catalogue · the coded track's Stripe objects were never minted at all · dry run against the sandbox shows five products absent: Portfolio website build, Portfolio admin panel, Logo or wordmark refresh, Extra page (portfolio), Booking setup. Not a run-order problem on its own — there was nothing to write ids from. `--apply` not run; awaiting Taylor.

2026-09-01 · catalogue · Stripe `tax_code` was only sent on `products.create`, so a product that already existed kept whatever code it was born with · every reassignment in Taylor's ratified table would have been a silent no-op against a live account. `setup-stripe-catalogue.ts` now compares the live code and updates it (Products are mutable, unlike Prices). Dry run reports 7 corrections, including "Blog post, written for you" sitting on txcd_10000000 (General - Tangible Goods) for a writing service.

2026-09-01 · catalogue · tax codes are NOT mirrored onto `products` in the database · Stripe resolves a line's treatment from the Product, nothing in the app reads the code, and `agora-invoicing.ts` already states the rule. A second copy would be a fact with two homes and no reader. Taylor's instinct, confirmed.

2026-09-01 · catalogue · `setup-stripe-catalogue.ts` kept its own parallel CATALOGUE — every product name, description, and amount existed twice and the two copies could disagree silently → it now imports `CATALOGUE_ROWS` and `STRIPE_PRODUCTS` from `seed-products.ts` and only regroups them into Stripe's Product-with-Prices shape. Taylor's instruction: the seed file is the source of truth. Verified by diffing the dry run before and after — byte-identical.

2026-09-01 · catalogue · `seed-products.ts` no longer runs on import (its `main()` is now an exported `seedProducts()`), and `applyTierEnv()` moved to the new `scripts/db-seed.ts` entry point, which `yarn db:seed` now targets · a module that seeds the database as a side effect of being asked what the catalogue contains cannot safely be imported by the Stripe script.

2026-09-01 · catalogue · every Stripe Product renamed to a track prefix — `Platform · …` for durable, `Coded · …` for real-code — and "Portfolio" removed from all product names, row names, and descriptions · Taylor's ruling, third time of asking: the coded track sells to consultants, speakers, and studios, so naming the product after one of its buyers misdescribes it for the rest. 18 products, 13 renamed and 5 created.

2026-09-01 · catalogue · `STRIPE_PRODUCTS` entries carry `previousName`, and `setup-stripe-catalogue.ts` falls back to it and renames in place · products are matched by name, so without it the rename would have orphaned 13 existing Products and minted 13 duplicates carrying none of the payment history. Verified by dry run: 13 `~ product` renames, 5 creates, zero duplicates.

2026-09-01 · catalogue · prices removed from the two build products' client-facing descriptions, and the amount removed from the two negotiated-rate Price nicknames · a price stated in prose is a second copy of a fact the Price already carries, and it goes stale on a receipt the client keeps. Payment structure ("half to start") is not restated either — it already lives on the Price nicknames. The nickname edits are free: neither price has been minted yet, so nothing is archived.

2026-09-02 · ADM-4 commit 1 · the per-kind presence facts moved from four step components into `lib/intake/showcase-kinds.ts` · `asksRoles`, `asksPortrait`, `asksVideo`, and `work` are now fields on `KindEntry`, read through `kindAsks()` and `workShapeFor()` in `lib/intake/tracks.ts`; `ENTRY_KEY` is deleted from `step-work.tsx` and `showsReel` derives from the work shape. Two of the four old comparisons were spelled identically (`kind === "portfolio" || kind === "practice"`) and meant different things — the roles question and the portrait question — which is precisely the drift a registry prevents, and the admin overview ADM-4 builds cannot compute "who is asked this" from a component's private opinion. Behaviour-preserving; `yarn verify:tracks` gains seven assertions pinning the exact mapping as it stood before the move. Ratified by Taylor 2026-09-02 (M-ADM-7); mirrored in the PORT log because `lib/intake/` is that track's file.

2026-09-02 · ADM-4 commit 1 · `eslint.config.mjs` ignored `.next/**` but not `.next-agent/**` or `.next-build/**` · `yarn lint` reported 16,573 problems (2,385 of them errors) the moment either `:agent` script had been run once, every one of them from a generated bundle and none from source. The `:agent` dist dirs are mandated by repo `CLAUDE.md` and were added without reaching the ignore list. Fixed in passing because ADM-4's Acceptance 10 requires `yarn lint` to pass; source was already clean and remains so (exit 0, no output).

2026-09-02 · ADM-4 commit 2 · the `file-drop` entry-key id fix · ADM-4's "Every kind" view renders sibling branches on one page, so the id audit it requires surfaced a duplicate that turned out to be a live client defect rather than a preview-only collision. Full detail in `docs/websites/specs/DEVIATIONS.md` — PORT owns `app/websites/intake/_components/file-drop.tsx`, so the reasoning lives there and this line is the cross-reference. Ratified by Taylor 2026-09-02.

2026-09-02 · ADM-4 commit 3 · the placement table listed eight primitives each gaining a document branch → a ninth file, `app/websites/intake/_components/document.tsx`, holds the three shared pieces (`DocTag`, `DocHint`, `DocOptions`) · the tag, the hint, and the option list are the same three shapes in every primitive, and eight copies of a `font-mono text-[10px] tracking-[.12em]` string is precisely how a register drifts away from the law that defines it (ADMIN-UX-SPEC §6.1). The spec's "Dev's call" anticipated the choice between a sibling function per primitive and a shared helper; this is both — a sibling function per primitive, built from three shared atoms. No question's words live in the new file.

2026-09-02 · ADM-4 commit 3 · the placement table said `ink-cluster.tsx` gains a document branch and `"use client"` → it is untouched · it renders a `<section>`, a mono label, and an intro paragraph, all of which are already prose and already correct in a document. Adding a context read to make it identical to itself would have made a Server-Component-capable file a client leaf for nothing. `field.tsx` and `known-fact.tsx` did gain `"use client"`, as specified, because both genuinely branch.

2026-09-02 · ADM-4 commit 3 · `useIsDocument()` added alongside `useRenderMode()` on the preview context · every primitive branches two ways, not three: a component rendering a document never also needs to know it is in preview, because a document renders no control there is anything to disable. `useRenderMode()` is kept for the admin surface, which does need the value itself.

2026-09-02 · ADM-4 commit 3 · preview-mode.tsx's header claimed "exactly three modules read this" → it now states the audit as re-run 2026-09-02: fourteen network callers, ten gated, four shell-only and unguarded by placement · the four (`complete-on-arrival`, `record-step-reached`, `send-my-link-button`, coded `step-proposals`) are mounted by `[token]/[step]/page.tsx`, which the preview never mounts. Naming them is the point: the next session can tell an intentional gap from an oversight.

2026-09-02 · ADM-4 commit 4 (part) · `Reveal` ships with a fourth condition shape, `{ extra }`, alongside the three answer-driven ones · the durable step 9's booking and Stripe blocks are opened by a purchased add-on rather than by an answer, and the preview buys nothing — so those questions were absent from the review surface entirely, not merely closed. Naming the add-on is not question copy: it is the product key the catalogue already carries.

2026-09-02 · ADM-4 commit 4 (part) · six hand-rolled checkboxes became `CheckAnswer` / `CheckRow` · the markup existed six times across both tracks with two alignments and one bare variant, and none of the copies had a document rendering. The `setValue`-then-`flush` pairing on toggle is preserved verbatim at each site: a checkbox has no blur to save on.

2026-09-02 · ADM-4 commit 4 (part) · thirteen answer-driven reveals now declare their condition as data · coded: the roster, the domain block, the logo file, the organisation "other" box, an offering's price, an ask's destination. Durable: insurance type, the deposit amount, the team block and its nested roster, the three domain framings, the booking and Stripe add-on blocks, and the second-Stripe-account warning. Each replaced a ternary over a derived boolean, and each derived boolean was deleted rather than left beside its replacement — a second copy of a condition is the thing D-ADM-10 exists to prevent.

2026-09-02 · ADM-4 commit 5 · the placement table named `_components/pack-diff.tsx` beside `pack-diff.ts` → the component is `pack-diff-section.tsx` · TypeScript resolves `.ts` before `.tsx` for one basename, so `import { PackDiffSection } from "./pack-diff"` silently resolved to the data module and reported a missing export. Mason's C3 (keep the pure walk co-located) is honoured; only the filename moved.

2026-09-02 · ADM-4 commit 5 · `PrimerBlock` is now mounted by `QuestionStack` on the about step · it is mounted by the coded shell (`[token]/[step]/page.tsx`) rather than by `StepAbout`, so a surface that renders step bodies never showed the business primer — and its paste box stores `businessPrimer`, a real step-1 answer (PORT-10). It already read `useIsPreview` and refused to run, so mounting it added no write path; it joins the gating audit list as a preview-mounted caller. `StepProposals` stays unmounted: a preview has no proposals.

2026-09-02 · ADM-4 commit 5 · the start form's two kind-driven blocks (`asksName`, `asksDisciplines`) are NOT wrapped in `ForKinds` · on that screen the kind is live user state rather than a fixed engagement kind, and with nothing selected both blocks are correctly hidden. Wrapping them would have made the overview's default kind decide a public page's behaviour before anyone had chosen one — a client-path change in service of an admin view. The overview covers the questionnaire, which is what was asked for.

2026-09-02 · ADM-4 commit 6 · Acceptance 4 as written ("outer HTML byte-identical apart from two id attributes") was restated · under a provider, interface mode legitimately differs wherever ADM-2's inert states apply — disabled uploads, disabled paste buttons — which is ADM-2 behaviour and not an ADM-4 change. What was run and what passed: (a) at step level, on two steps with no upload or paste control, provider-interface is byte-identical to the client route; (b) across both tracks' full stacks, the only differences are ADM-2 inert-state strings, and no document artifact (`asked of:`, `Shown when`, a component tag) leaks into interface mode; (c) `Reveal` and `ForKinds` emit no element in interface mode, so a wrapped branch adds no DOM. The two intended id changes are the `file-drop` entry key and the `f-fastWay` mode suffix.

2026-09-02 · ADM-4 commit 6 · the acceptance sweep found two primitives with no document branch — `ExtractionBlock` and `PrimerBlock` — and both were added before close · the paste boxes are questions (`fastWay`, `businessPrimer`) and would otherwise have rendered their full interactive card inside the document. Found by the sweep rather than by reading, which is the argument for running it across all eight views and both modes rather than spot-checking one.

2026-09-03 · ADM-4 follow-up (Taylor) · Copy-as-Markdown was cut to a pin (§Out of scope) → a **Download Markdown** button ships instead · Taylor asked for a downloadable file, so the pin is delivered rather than deferred. Mason's objection to the cut version — that a walker's fidelity depended on a `data-md` vocabulary and a walker agreeing, with nothing checking the agreement — is closed by shape rather than by argument: the conversion is a pure `htmlToMarkdown(html, meta)` in `to-markdown.ts`, separate from the button that calls it, so a test renders the real document, converts it, and asserts every question and every option survives. Two defects were found that way and neither would have been found by reading (see the next two lines).

2026-09-03 · ADM-4 follow-up · `htmlToMarkdown` treats any element carrying `data-md` as a block boundary, not just `<p>`/`<h*>`/`<li>` · the answer key is a `<span>` beside its heading, so with tag-name-only boundaries its text stayed in the buffer after the heading closed and was swallowed into the next paragraph — the key vanished from the file and a help line silently gained a word.

2026-09-03 · ADM-4 follow-up · `data-md="skip"` suppresses its own content but is deliberately **not** a boundary · the first attempt made it one, and because the decorative bullet glyph sits inside its list item and before that item's text, it closed the item early and threw away the option. 55 of 70 options were lost. Both this and the line above were caught by the conversion test on its first two runs.

2026-09-03 · ADM-4 follow-up · the export reads the accordion a concurrent session added the same day · step and band titles are `<span>`s inside the toggle button (they must be phrasing content), so they carry `data-md="section"` and their eyebrows `data-md="eyebrow"`; the accordion's own Hide/Show state word carries `data-md="skip"` so a control's label never lands in the file. That session had already set `keepMounted` in document mode for exactly this reason, so a collapsed step still exports.

2026-09-03 · ADM-4 commit 5 superseded · the `PrimerBlock` mount added to `QuestionStack` was removed by a concurrent session, which moved the block into `StepAbout` itself · the better fix, and for a reason this ticket did not know: mounted beside the step by the route shell, the primer had no autosave hook of its own (two hooks cannot share the `about` step key), so its paste never reached the database. The review surface now shows it because the step does, which is where D-ADM-6 wanted it all along.

2026-09-03 · D-ADM-13 (Taylor) · the "no new dependency beyond `lucide-react`" non-negotiable is amended by `next-themes@0.4.6` · Taylor asked for a Light / Dark / System control built on the shadcn, Next, and Tailwind conventions, and that convention is this package; a hand-rolled equivalent was considered and rejected (M-ADM-8). README non-negotiable reworded to name both. Ratification of the dependency is the one open item on this ticket. Verification run recorded in the same session: `tsc --noEmit` clean, `yarn lint` clean, `yarn build:agent` green; server HTML of `/admin/login` carries the theme script before the `.admin-theme` marker and before the first heading, so there is no unthemed frame.

2026-09-03 · D-ADM-13 · first pass declared the six admin surface tokens in the plain `@theme`, and the dark login page rendered its inputs with a transparent fill and a body-coloured border · the tokens are consumed only through `bg-(--color-well)`-style references, which a plain `@theme` does not count as consumers, so they never reached `:root` — the exact trap the night-ramp comment in `globals.css` already records. Moved to `@theme static`. Caught by measuring computed styles in the browser, not by reading: `tsc`, lint, and the build were all green with the defect in. Browser verification after the move, on `/admin/login` against the shared dev server: dark — input `rgba(0,0,0,.3)`, border `rgba(255,255,255,.15)`, ink `#f1effa`, gradient and grain present, `color-scheme: dark`; light — body `#fff`, ink `#060b1e`, dim `#6b6889`, well `rgba(6,11,30,.04)`, line `rgba(6,11,30,.15)`, grain off, `color-scheme: light`. Gate: removing the `.admin-theme` marker with `.light` still on `<html>` returned every value to dark. Storage: `ta-admin-theme=dark` under a light system preference produced `.dark`. No console errors. The rail control itself was not exercised in a browser — the in-app browser has no admin session and Chrome was not connected — so its placement and markup were verified by a server render of `AdminSidebar` instead; a logged-in screenshot is the one check still owed.

2026-09-03 · D-ADM-13 feedback (Taylor, on `/admin/intake/questions` in light) · toggle goes icon-only; `GradientButton.tsx` loses its two inline `rgb()` values and its c2/c3 fill in favour of `--color-gold-500`, `--color-gradient-hot`, and four `--color-ghost-*` tokens (dark values unchanged, `Pricing.tsx` and `step-progress.tsx` converged onto the same pair); light dim lifts from D-DOC-1's `#6b6889` (4.9:1 on the tint) to a proposed night-650 `#5a5773` (6.4:1) · the ghost's grey was `ground-a` at 55% over paper — a hardcode the token sweep could not reach because it was an arbitrary-value class, not a `white/N`. The fix is the same shape as M-ADM-9: one home per fill, and the light block overrides the home.

2026-09-03 · D-ADM-13 follow-up (Taylor, linked ui.shadcn.com/docs/components/base/toggle-group) · the theme toggle is restyled as a connected segmented `ToggleGroup` — shared border, dividers between cells, ends rounded — with roving tabindex and arrow-key focus movement copying Radix's real ToggleGroup contract, hand-built rather than adding `@radix-ui/react-toggle-group`. Three cells did not clear the bar for a second Radix package on top of `@radix-ui/react-dialog`, and the rail's own section flyout already set the precedent of a CSS variant over a second overlay dependency (TECH-SCOPE §5, M-ADM-4 context). Verified: server render shows exactly one `tabindex="0"` per width, the other two at `-1`; visual check in the browser (dark and light, row and column) shows the shared border, dividers, and end rounding matching shadcn's outline segmented look, with dark pixel-identical to before this change.

2026-09-03 · D-ADM-13 follow-up (Taylor: "too big and blocky") · the theme toggle drops from 44px cells on `--color-line` to 28px (`size-7`) cells on `--color-faint`, the site's standard hairline, with a 14px icon. Below the rail's own 44px nav-target floor (ADM-1-admin-shell.md §"Targets at or above 44px") by explicit instruction — a settings control touched occasionally does not carry the same weight as primary navigation, and `/admin` is a desktop tool with one user, not a touch surface. Scoped to this control only; the 44px floor is unchanged everywhere else in the rail and the rest of the admin. Verified in the browser: dark and light, row and collapsed-column, at 3x zoom — border, dividers, end-rounding, and the selected cell's tint fill all still read cleanly at the smaller size.

2026-09-03 · D-ADM-13 follow-up (Taylor: "edges are a bit sharp... relationship to the left padding is off... be a professional") · three fixes, no size change from the prior pass. Corners: the toggle read as a foreign shape next to the rail's own `rounded-md` (Collapse, Sign out, every nav hover) because it was on the public site's 3px `--radius` law instead — switched to `rounded-md` to match the family it sits in. Alignment: the toggle's wrapping div carried no horizontal inset, so its left border sat 8px inboard of the Collapse icon and the email text, which both get to their left edge through a button's or paragraph's own `px-2` on top of the footer's `p-2` — added the matching `px-2` to the toggle's wrapper. Rhythm: the gap after the toggle was a bare 4px `mb-1` against roughly triple that above it (Collapse's own 44px height read as breathing room the compact toggle didn't have); wrapper now carries `mt-2 mb-2`, which collapses against Collapse's `mb-1` to a measured 8px on both sides. Verified by DOM measurement (not eyeballing): toggle border-left and Collapse icon left both at the same x; gap above and gap below both exactly 8px.
