# PORT — Deviations (append-only)

One line per intentional divergence from a spec or the handoff. House format, exactly:

```
YYYY-MM-DD · <ticket-id> · <what changed> · <why>
```

On-disk reality plus this log override any stale string in the corpus. Never rewrite history.

---

2026-08-26 · authoring · All nine PORT tickets authored in one session, over the guide's three-per-thread ceiling · Same mitigation as INT's authoring batch: one shared tech scope, one governing UX scope, and one copy authority (the v2 doc) across the set.
2026-08-26 · PORT-3 · Care plan row removed from the pay screen, departing from the v2 doc's four-row add-on menu · Taylor's ruling "don't charge for the maintenance care plan until it's done"; nothing recurring is sold in v1 (M-PORT-6). Restoring it is a seed flag plus the v2 row's copy.
2026-08-26 · PORT-3 · Ticket amended after authoring, before execution · Its three blocking decisions were ruled by Taylor the same day (M-PORT-6); a not-yet-started ticket whose gate resolves gets corrected at the source rather than carrying stale `[NEEDS DECISION]` text into a build thread.
2026-08-26 · execution · Two tickets per build thread instead of one · Batch plan in `00-build-order.md`; the per-thread rule's purpose is preserved by a hard checkpoint between tickets — three-place closure, a five-line report, and a context clear before the second attach-list is read.
2026-08-26 · PORT-1 · `lib/intake/steps.ts` keeps only the durable registry; its helper functions moved to `lib/intake/tracks.ts` track-parameterized · M-PORT-1 gives step identity one home per track and one resolver across tracks; leaving parallel helpers in the durable file would have been the second home the decision exists to prevent.
2026-08-26 · PORT-1 · Showcase registry lives in its own `lib/intake/showcase-steps.ts` rather than inside `tracks.ts` · Mirrors the durable track's file-per-registry shape and keeps `tracks.ts` a seam rather than a content file; M-PORT-1's "one home per track" is satisfied either way.
2026-08-26 · PORT-1 · Step 4's showcase intro is composed from a flavoured lead sentence plus a shared tail · The v2 doc gives the flavour variants as a replacement for the opening sentence only, eliding the rest; composing keeps both halves verbatim instead of restating the paragraph per pack.
2026-08-26 · PORT-1 · The durable step page resolves the engagement before the step slug (order reversed) · Which registry a slug belongs to cannot be known without knowing the engagement's track. A bad token on a bad slug now shows the link-unavailable screen rather than a 404; both are dead ends and neither leaks existence.
2026-08-26 · PORT-1 · `collectFlags` returns empty for non-durable tracks · Every condition in it is written in durable field vocabulary; the showcase flags arrive with PORT-8. A flag reading the wrong track's answers would be silently absent on exactly the engagements it exists to protect.
2026-08-26 · PORT-1 · `labelFor` removed from `lib/intake/answer-labels.ts` · Superseded by the track-aware `labelFor` in `tracks.ts`; leaving it would have been a second, track-blind way to label an answer.
2026-08-26 · PORT-1 · `scripts/verify-track-cartridge.ts` added (`yarn verify:tracks`) · The cartridge is pure, so its proof is a script rather than a click-through; `--document` mode is the durable regression oracle for every later slice that touches a shared seam.
2026-08-26 · batching · Batch 1 re-cut to PORT-1 alone; PORT-2 becomes batch 2 · PORT-1's migration, seam extraction, and byte-for-byte regression proof were a full thread's reasoning load on their own. Sizing corrected against evidence rather than held to.
2026-08-26 · PORT-2 · Built at `/websites/coded/intake` with no redirect stub, superseding the ticket's `/websites/portfolio/intake` · M-PORT-7; the coded-track sales page already exists and is the explainer a trimmed URL lands on.
2026-08-26 · PORT-2 · `ShowcaseWelcome` and the showcase done page are sibling components rather than parameterizations · M-PORT-9 — they share no copy with the durable versions, and a component whose every string is a prop cannot be proofread against the approved doc.
2026-08-26 · PORT-2 · `ChoiceGroup` gained an optional per-option `disabled` flag · The coming-soon category rows must be announced and focusable but unselectable; `aria-disabled` rather than `disabled` keeps them in the tab order so the roadmap is not sighted-mouse-users-only. Durable call sites are untouched.
2026-08-26 · PORT-2 · The showcase track uses its own resume cookie (`ta_coded_intake`) scoped to its own prefix · One token is one engagement on one track; a browser that has started both must never send either token to the other tree.
2026-08-26 · PORT-2 · `findResumableByEmail` is filtered by track at the call site rather than in the service · Someone who bought a platform site last year and is now buying a coded one is starting a genuinely new engagement; the service stays track-agnostic so the durable form's behaviour is unchanged.
2026-08-26 · PORT-2 · `buildIntakeStepUrl` gained a track parameter and `buildEntryUrlFor` was added · The reminder sweep spans both tracks and was composing durable URLs for every engagement; a showcase client would have been mailed a link into the wrong tree.
2026-08-26 · PORT-2 · A `PayPending` placeholder stands in for the showcase pay screen · PORT-3 owns the real one and deletes this file. Without it an unpaid showcase engagement would fall through to the questionnaire, which is a $2,000 build given away.
2026-08-26 · PORT-2 · Showcase step pages render an empty body · Per the ticket: the step slices (PORT-4/5/7) fill them; this slice proves the frame against the real registry.
2026-08-26 · PORT-3 · Cross-track promo leak found and fixed during verification · The platform track's $0 grant validated on a coded-track order (M-PORT-11). Pre-existing hole widened by this slice; closed on both the display and charge paths.
2026-08-26 · PORT-3 · The promo rail was extracted to a shared `promo-rail.tsx` used by both pay screens · The two rails are word-for-word identical; a money surface with two copies of the same markup is two places for a disabled state or a focus ring to drift. Durable P0 regression-tested in a browser after the extraction.
2026-08-26 · PORT-3 · Showcase add-on rows ship without "How it works" dialogs · The v2 doc says each row has an info button but supplies no dialog content, and `content/addon-details.ts` entries are three screens of new client-facing copy. Rows render correctly without them (an add-on with no entry simply shows no button). Queued for Taylor rather than invented.
2026-08-26 · PORT-3 · The refund sentence is absent from the coded pay screen · D-INT-10 ruled one for a $600 deposit; this screen can charge $1,900 and Taylor has not ruled the equivalent. Rendering the other track's promise would be inventing a refund policy.
2026-08-26 · PORT-3 · Coded-track Stripe objects are not minted; their seed rows carry null price ids · `yarn stripe:catalogue --apply` is Taylor's to run. A row without a price id is not sellable, so the screen degrades to offering nothing rather than to a checkbox that cannot be charged.
2026-08-26 · PORT-3 · Checkout success/cancel URLs and the pay-in-full option resolve per track · A coded client returned to the platform entry route would be told their link was unavailable seconds after paying; a pay-in-full card with no sellable row behind it would price something nothing could charge, so it is not rendered.
2026-08-26 · batching · Batch 3 split at its checkpoint; PORT-4 becomes batch 3b · Permitted by the batch rule and exercised for its stated reason: PORT-3 ran long (a cross-track promo leak found mid-verification, four settlement paths plus replays), and PORT-4's failure mode is silent copy paraphrase across ~50 verbatim fields — the specific work not to begin on a thread that has already spent its care.
2026-08-26 · PORT-4 · Showcase autosave was silently broken and is fixed · `saveStepInput` validated against the durable step enum only, so every showcase save would have failed shape validation and retried forever (M-PORT-13). Found in a browser; the action's `stepKey: string` signature hid it from the type checker.
2026-08-26 · PORT-4 · Step 1's three contact fields render as shown facts, not editable inputs · They are engagement columns, and an answer diverging from the row would put two different emails in two different systems. The screen says to tell Taylor instead. `[COPY — pending Taylor]` on that one line.
2026-08-26 · PORT-4 · Step 2 is grouped into three hairline-separated clusters · Nine long-text boxes in a row is where a creative stops at the fifth; the v2 doc's field order is unchanged, only its rhythm.
2026-08-26 · PORT-4 · The flavoured accounts help on step 9 reads from `copyPackFor` rather than a local string · One home for the four strings that differ between film and generic; PORT-5 and PORT-7 read the same pack.
2026-08-26 · verification · `yarn build` and `yarn dev` cannot share `.next` · A production build followed by a dev server produces ENOENT and phantom 404s on freshly added routes. Clear `.next` between them. This caused two false alarms across PORT-3 and PORT-4 before being identified.
2026-08-26 · PORT-5 · The reel label ships as plain text, dropping the v2 doc's italic on "the" · A field label is a string, not markup. The words are verbatim; only the emphasis is absent. Restoring it means widening the label primitive to ReactNode — a shared-component change not worth making inside this slice.
2026-08-26 · PORT-5 · Project images are read in one query and grouped client-side by entry key · One query per project would be forty round trips to render one screen for the catalogue this track exists to serve.
2026-08-26 · PORT-5 · The share-password field renders `type="text"` with `autocomplete="off"` · It is not a credential (handoff law 3). `type="password"` would invite a password manager to offer something that *is* one, which is the confusion the no-passwords law exists to prevent. Verified in a browser.
2026-08-26 · PORT-5 · The extraction button ships disabled with a placeholder line · PORT-6 makes it live. The blob autosaves from the moment it is typed regardless, which is the safety contract; an enabled button that silently does nothing would be worse than an honest one that waits. `[COPY — placeholder, removed by PORT-6]`
2026-08-26 · verification · A second dev server from another session shares this repo's `.next` · Root cause of the phantom 404s and ENOENT errors seen across PORT-3, PORT-4, and PORT-5. Not fixable from inside one session; clear `.next` and retry, and do not kill the other process.
2026-08-26 · PORT-5 · Upload issuance not exercised end to end · The scratch environment has no `intake` storage bucket, so issuance returns 500 after passing validation. What was verified: oversize refused at 400 by the schema before any storage call, odd formats passing validation, and the entry-key grouping proven with seeded rows. The bytes-to-storage path still needs a real bucket.
2026-08-26 · PORT-6 · Extracted entries commit on the autosave debounce, not on blur or step change as D-PORT-3 and TECH-SCOPE §8 stated · The engine saves on change too; the spec described a mechanism it does not have (M-PORT-16). Shipped as-is, record corrected, copy question routed to Taylor.
2026-08-26 · PORT-6 · `@anthropic-ai/sdk` pinned at 0.121.0 · Used in exactly one file. Structured output via `messages.parse` + `zodOutputFormat`, so the response shape is validated by the same Zod definitions the form uses rather than parsed from free text.
2026-08-26 · PORT-6 · The extraction prompt lives beside the service, per mode · It is the only defence against a plausible invention — a festival that never selected them — that no code-level law can provide, so it says "omit rather than guess" explicitly.
2026-08-26 · PORT-6 · Five new client-facing strings ship draft-marked `[COPY — pending Taylor]` · The running, empty-paste, rate-limited, expired-link, and failure lines. The v2 doc supplies the two after-lines, which ship verbatim.
2026-08-26 · PORT-7 · Found and fixed silent write-loss on array answers · Three rapid favourite toggles produced one favourite; `setValue` now accepts an updater (M-PORT-17). Durable autosave regression-tested after the shared-hook change.
2026-08-26 · PORT-7 · Example sites ship as a labelled STUB set of six with generated placeholder captures · Curation is Taylor's (handoff decision 7). `content/intake-examples/film.ts` carries the curation guidance and a "NOT REAL SITES — nothing here may ship to a client" header; generic borrows the film set until a second one exists.
2026-08-26 · PORT-7 · Notes are kept when a site is unfavourited, and carried back when it is re-favourited · "This one, but not the type" is a real instruction whether or not the site made the shortlist. The note lives on the favourite while ranked and in a `notes` map while not — one place at a time.
2026-08-26 · PORT-7 · The dark/light help renders the base string for both packs · Its only v2 variant is a photography/art one that has not been written; it joins the copy pack when it exists rather than branching in the step.
2026-08-26 · PORT-7 · The three "feel like" boxes carry per-input `aria-label`s · Three identical unlabelled fields under one group label is what a screen reader would otherwise announce.
2026-08-26 · verification · Browser-pane measurements read zero until a viewport is set explicitly · `resize_window` first, or every `getBoundingClientRect` reports 0 and looks like a layout bug. Cost one false alarm here.
2026-08-26 · PORT-8 · Showcase flags are five conditions, not the six the ticket sketched · "Rights not cleared" covers both the ask-me and not-sure answers in one line, named and counted, rather than two flags saying the same thing. Each was verified to stay silent when its trigger is absent.
2026-08-26 · PORT-8 · Project images render under their project and are excluded from the Files section · Listing them twice doubles a forty-project catalogue's file list for no gain.
2026-08-26 · PORT-8 · Email variants are one noun and one duration, not a second email system · Cadence, ceiling, dedupe, and register stay one mechanism across both tracks; a second system is a second place for the three-reminder ceiling to be got wrong. `[COPY — pending Taylor]` on "portfolio questionnaire".
2026-08-26 · PORT-8 · The done screen needed no work · PORT-2 built it with the v2 copy verbatim, and `collectUnanswered` became track-aware in PORT-1, so the skipped list started telling the truth as soon as the schemas had fields.
2026-08-26 · PORT-8 · Label sweep run and clean · Every field in every showcase step schema has a label; a missing one would render in the document as its raw key.
2026-08-26 · PORT-9 · Extra pages use one basket row with `quantity`, not one row per page · The first attempt violated `engagement_products_once_idx`, which predates this work; the table already had a `quantity` column (M-PORT-20). Found by running against a real database.
2026-08-26 · PORT-9 · A repeat extra-page purchase is refused, not recorded · A paid row is history and is never rewritten. The runbook routes it to Stripe by hand.
2026-08-26 · PORT-9 · A `showcase_extra_page` row was added · `extra_page` is a durable-track row, and charging a coded client from it would be the same cross-track leak PORT-3 closed. Same $150, own row, never offered at checkout.
2026-08-26 · PORT-9 · Settlement branches on `charge_kind` metadata so ancillary money never sets `paid_at` (M-PORT-19) · Verified on both tracks including replays.
2026-08-26 · PORT-9 · The care plan and the balance ship as runbook sections, not code · The care plan has no settled offer and no Stripe object (M-PORT-6); the balance already rides the existing invoicing rail and needed nothing.
2026-09-01 · authoring · PORT-H1 and PORT-11…17 authored in one session, over the guide's three-per-thread ceiling · Same mitigation as the first nine: one governing UX scope (`../CODED-INTAKE-KINDS-UX-SCOPE.md`), one audit as the finding of record, and one copy source (the scope's §6 tables) across the set. Mason's four placement calls were recorded as M-PORT-21…24 at authoring rather than at closure so the tickets could cite them.
2026-09-01 · PORT-12 · Draft client-facing strings ship marked `[COPY — pending Taylor]` rather than gating the slice on Taylor's human-hand pass · Holding PORT-13/14/15 behind ninety string edits parks the venture path; the pass edits one file (`lib/intake/showcase-copy.ts`) whenever it happens. PORT-6 precedent.
2026-09-01 · execution · Phase 6 continues the two-tickets-per-thread batch rule with the hard checkpoint between them · Batches are listed in `00-build-order.md` § Phase 6; PORT-14 and PORT-17 run alone by design.
2026-09-01 · PORT-H1 · The interpolation helper landed in `lib/intake/showcase-steps.ts` (re-exported through `tracks.ts`) rather than inline in `step-words.tsx` · The ticket's Dev's-call line offered inline or beside `copyPackFor`; the registry keeps the verb per pack, which is what PORT-15 needs, and the registry's one-importer law meant the seam had to re-export it rather than the component importing it directly.
2026-09-01 · PORT-H1 · `ShowcaseCopyPack` gained `personVerb` and `personVerbThird` · Two stored forms rather than one derived by appending an "s": correct for direct/directs and make/makes, and already correct for the service pack PORT-15 adds, where "do" would become "dos".
2026-09-01 · PORT-H1 · A consumer outside the ticket's Placement section was updated: the admin question-review surface `app/admin/(protected)/intake/questions/_components/question-stack.tsx` · It renders every showcase step and already passes `flavour` to StepWork, StepTaste, and StepAccess; StepWords now needs it too. `displayName` is deliberately not passed there — that surface reviews questions rather than one client's answers, so the third-person example renders its no-name form. Found by `npx tsc --noEmit`, not by inspection.
2026-09-01 · PORT-H1 · Two "Kryshan" strings remain in the repo, both in code comments · `lib/intake/promo.ts:38` (pre-existing, records a real negotiated price) and the new docstring in `showcase-steps.ts` naming the defect this function exists to prevent. The non-negotiable's own check — no client name in a shipped string — passes; comments are not copy. Overruling is one edit.
2026-09-01 · PORT-11 · The answer key is `about.siteKind`, not `about.kind` as the spec and M-PORT-21 both said · Step 4's project entry already owns the field key `kind` ("What kind of thing it is") and `SHOWCASE_ANSWER_LABELS` is flat — one label per key across the whole track — so a second `kind` would have silently relabelled every project in every intake document. Caught by the duplicate-key collision, not by review. Amended in TECHNICAL-DECISIONS as M-PORT-25.
2026-09-01 · PORT-11 · `content/intake-examples/index.ts` was edited although the ticket's Placement section does not name it · Widening `ShowcaseFlavour` from two packs to six made its exhaustive `Record<ShowcaseFlavour, …>` incomplete. The four new packs point at the same stub set the existing two already point at, so behaviour is unchanged for every engagement. PORT-16 owns that map and deletes the stub.
2026-09-01 · PORT-11 · One pack slot beyond step 4's title and intro was filled here, against the slice's own non-negotiable · `whatYouDoLabel` ("What it is, in one line") for the entity, venture, and service packs. Acceptance criterion 1 requires the one-liner label to flex on the start form, which cannot happen without it; the criterion is the objective and the non-negotiable's letter lost. The remaining ~89 strings are still PORT-12's.
2026-09-01 · PORT-11 · `WORK_INTRO_TAIL` moved from a module constant onto the copy pack as `workIntroTail` · The venture pack needs its own tail (kinds scope §6.4) and a constant beside the registry cannot flex. Both halves of the v2 paragraph are still verbatim.
2026-09-01 · PORT-11 · The start form's `siteKindsOther` free-text input was removed · "Something else" is now one radio option among six rather than a checkbox needing elaboration, and the kinds scope §3 field order has no free-text under it. The key stays in the schema and the label map, read-only, so engagements that answered it still render it.
2026-09-01 · PORT-12 · The start form was edited although the slice's criterion 5 says no component file changes · PORT-11 had left `kindHelp` and the entity-name placeholder as literals on that screen; landing them in the pack would have created two homes for one string, and **no remaining Phase 6 ticket owns the start form** (PORT-13 is step 1, PORT-15 is steps 2–9). The one-fact-one-home law outranks the criterion. Its kind label and entity-name label moved too; every string left on that screen is now approved v2 copy.
2026-09-01 · PORT-12 · Step 2's intro became a pack slot, joining step 4's title and intro in the registry · It flexes for the entity, venture, and service packs (kinds scope §6.2) and the registry is where a step's intro already lives. `showcaseSteps` now reads two pack slots instead of one.
2026-09-01 · PORT-12 · Four slots are optional on the pack type rather than required with a generic floor · `basedInHelpSuffix`, `notableNamesHelpSuffix`, `voiceNoteSkipSuffix`, `reachHelpSuffix` are appended to existing help lines, and an empty string would be a value a proofreader has to read past. `stage` is optional for the same reason: it is asked of ventures only.
2026-09-01 · PORT-12 · The generic pack carries an entity-name placeholder although kinds scope §6.1 gives that cell as "—" · The "Something else" kind resolves to the generic pack and *is* asked the question, so a blank would ship an unplaceheld field. The string is the entity column's, applied as the floor.
2026-09-01 · PORT-12 · `personSubject` was added to the pack and PORT-H1's helper now reads it · Kinds scope §4 step 6 gives the entity and service packs a "We" subject; PORT-H1 deliberately left it for later and this is the string-landing slice. PORT-15 no longer needs to touch the helper.
2026-09-01 · PORT-12 · Observation, not a change: the generic pack's step-3 paste intro still names IMDb, per kinds scope §6.3's "(v2)" cell · That is one of the film-shaped strings the audit flagged, and the copy scope did not give it a generic rewrite. Landed as tabled rather than invented; flagged for Taylor's pass.
2026-09-01 · PORT-16 · The taste step takes a resolved `ExampleSet` from its page rather than importing `examplesFor` itself · The studio kind reads entity copy while its gallery may follow its discipline, so gallery choice is a resolver question; `galleryFlavourFor` landed in `lib/intake/tracks.ts` and the client component stopped importing `content/` altogether.
2026-09-01 · PORT-16 · Step 5's dark-or-light help was wired to the pack here, which is PORT-15's scope · Replacing `examplesFor(flavour)` with the gallery prop left `flavour` unused and lint warning; the alternatives were a dead prop for one ticket or removing a prop PORT-15 adds straight back. One line, the string already existed from PORT-12, and it stops the film line ("most film sites run dark so the footage glows") reaching every non-film client — audit §3 step 5.
2026-09-01 · PORT-16 · `question-stack.tsx` updated again, for the same reason as PORT-H1 · The admin question-review surface renders the taste step and needed the new `gallery` prop. It passes `examplesFor(flavour)` with no discipline refinement, so an uncurated pack shows Taylor the absent state a client would meet.
2026-09-01 · PORT-16 · The render branch was not browser-verified · No database is reachable in this session, so no engagement could be minted to reach `/websites/coded/intake/[token]/taste`. What was verified: every pack resolves its own set, no set is curated-but-empty, no placeholder or unsized site exists in any set, and the branch type-checks. The one-line ternary is the whole behaviour.
2026-09-01 · PORT-13 · The roster fields were added to `stepAboutSchema` twice, because the first edit was a silent no-op · Its anchor comment had been rewritten by PORT-11, so `stage`, `justYou`, `showTeam`, `people`, and `leadPerson` were never added — and `npx tsc --noEmit` passed anyway, since every showcase field is optional. Caught by the new D-PORT-11 assertion in `verify-track-cartridge.ts`, which is the reason that assertion exists.
2026-09-01 · PORT-13 · `personEntrySchema` was declared after `stepAboutSchema` and had to move above it · A temporal-dead-zone error at module load that the type checker did not flag. Found by running the verifier, not by review.
2026-09-01 · PORT-13 · Step 1 derives its kind from the form rather than from the server prop after first render · The page hands down what the engagement resolved to on load, including the legacy derivation; the component then reads its own autosaved `siteKind`, so the Change link re-renders the questions immediately instead of needing a reload.
2026-09-01 · PORT-13 · The kind line renders outside `Field` when collapsed · `Field` always emits a `<label>`, and a known fact has no control to label. Open, the picker does use `Field` — `ChoiceGroup`'s own legend is `sr-only`, so the visible question has to come from somewhere.
2026-09-01 · PORT-13 · The admin question-review surface reviews step 1 as a portfolio · It renders one step per kind-less preview and the portfolio shape asks the most shared questions; the roster and stage are reviewed by switching its flavour selector. Third time this surface needed a prop a Phase 6 ticket added — it is a standing consumer of every step component and worth naming in the remaining specs.
2026-09-01 · PORT-13 · `people` renders in the intake document through the generic array path, not a section of its own · PORT-14 owns `renderEntries` and gives it a heading; until then it prints like the existing `accounts` array, which is legible but not sectioned.
2026-09-01 · PORT-15 · The documents promise lives in the copy pack's `documents.help` rather than as a constant beside `DocumentDrop` as the spec said · A constant would have been a second home for a string the pack already carries, and Taylor's copy pass only reaches the pack. `yarn verify:tracks` now asserts every pack's help still contains the promise — a guard instead of a copy.
2026-09-01 · PORT-15 · Step 5's dark-or-light help was already wired at PORT-16 · Logged there; noted here so the seven-step sweep reads as complete rather than as six.
2026-09-01 · PORT-15 · Steps 7, 8, and 9 gate their groups on the engagement's kind, not on its pack · The portrait, the video-host question, the reach additions, the documents drop, and the tools group all key off `kind` or `groupsFor(kind)`, per the slice's own ruling. `flavour` still supplies every string.
2026-09-01 · PORT-15 · The admin question-review surface reviews steps 1, 7, 8, and 9 as a portfolio · Fourth Phase 6 ticket to touch it. It has no engagement and therefore no kind; the portfolio shape asks the most shared questions and its flavour selector reaches the rest. **PORT-14 and PORT-17 will need it again** — it is a standing consumer of every step component.
2026-09-01 · PORT-14 · **The kinds scope's ask-block help named Holistica in shipped copy, and PORT-12 had already shipped "Holistica Properties" as a venture placeholder** · Both break D-PORT-14, the law PORT-H1 exists to enforce: no client's name is ever a literal in shipped copy, on any track. Replaced with neutral drafts ("A project raising money often has three…" and "Riverstone Commons"), both marked `[COPY — pending Taylor]`. **Routed to Taylor and Vesper:** the scope's §5.3 and §6.3 still carry the client's name and should be amended at the source.
2026-09-01 · PORT-14 · The ask-block and claims-cluster strings live in the copy pack, not in their components as PORT-12's note said · They do not flex between packs, but they are new and the copy pass has to reach them — and the build order now promises that pass is one file. Splitting it across two would have broken that promise. PORT-12's docstring corrected in place.
2026-09-01 · PORT-14 · Three ask fields are typed label-only rather than `CopyField` · "The ask", "How it should happen", and "Show it on the site?" are answered by their options; an empty help string would have been a value the pass has to read past, and inventing help the scope did not write is worse.
2026-09-01 · PORT-14 · A fourth flag was added beyond the three the spec named · An ask marked "Not yet" must not reach a live site, and the document is where that is caught. Same silence rule as the other three.
2026-09-01 · PORT-14 · `EntrySummary`, `CollapseLink`, and `EntryRow` were extracted to `entry-collapse.tsx` · Five entry shapes now need identical collapse behaviour. `ProjectEntryCard` keeps its own copy deliberately, so the portfolio path stays byte-for-byte what it was.
2026-09-01 · PORT-14 · `renderProjects` was generalised to `renderEntryArray` rather than replaced · The projects renderer has per-field rights and placement handling that the generic one does not need; keeping both means the portfolio document is provably unchanged, which the byte-identity check confirms.
2026-09-01 · PORT-14 · **The step-4 paste button shipped live rather than inert for the new kinds, and PORT-17 fixed it** · PORT-14's spec said the button would carry PORT-5's placeholder line until PORT-17; instead it stayed wired to `mode="projects"` and wrote to the `projects` array for every kind, so a venture's extraction would have been stored under a key its step does not render. Not answer loss — the value persisted and the document would print it — but invisible to the client. Caught while wiring PORT-17, which was the next ticket; had the two been separated in time this would have reached a client.
2026-09-01 · PORT-17 · Enum fields are typed as plain strings in the model's output schema, not as their enums · Typing `status` as its three values forces the model to pick one, and "the deck did not say" is the answer that matters most on exactly that field. The prompt carries the constraint instead, and the eval grades it.
2026-09-01 · PORT-17 · The roster's paste box stores to a new `peoplePaste` key rather than reusing `fastWay` · `fastWay` belongs to steps 3 and 4; a third consumer on step 1 would have put three different pastes in one field. Same D-PORT-3 contract: saved as an ordinary answer from the moment it is typed.
2026-09-01 · PORT-17 · The pack gained `rosterPaste` although the strings do not flex between packs · Same reason as PORT-14's ask block: they are new, and the build order promises Taylor's copy pass is one file.
2026-09-01 · PORT-17 · The eval's self-test caught a wrong expectation in its own author's assertion before any model ran · The grader was right and the test was wrong (three ungrounded values, not two). Recorded because it is the argument for writing the eval before the prompt.
2026-09-01 · PORT-17 · The live eval run was not executed · It needs `ANTHROPIC_API_KEY` and an `EVAL_ENGAGEMENT_ID` to spend the per-engagement counter against; neither exists in this session. **Taylor or the next thread must run `yarn eval:extract` before a client uses the new modes** — the prompts have never been graded against a model.
2026-09-01 · PORT-10 · Proposals are stored at `answers.primer`, not the `engagements.primer_proposals` column its type docstring named · No migration, and a top-level key is invisible to the intake document by construction rather than by an exclusion someone must remember. Recorded as M-PORT-26; the docstring in `lib/intake/primer-proposal.ts` still names the column and should be corrected when someone next opens it.
2026-09-01 · PORT-10 · Proposals are met in a per-step block above the questions, not inline per field · The ruling requires per step, where the question lives, and forbids one wall of suggestions; a block scoped to one step satisfies both and mounts once in the step page instead of in all eight step components. Inline-per-field would have meant plumbing the proposal set into every step's form state.
2026-09-01 · PORT-10 · Accepting a proposal writes server-side (M-PORT-27) rather than through the client's own autosave as PORT-6's entries do · The browser names a field key and the server looks up what was proposed for it, so a fabricated request can only accept something the primer actually produced. The seen-first law is satisfied by the client having read the suggestion and its quote before pressing.
2026-09-01 · PORT-10 · The live eval was not run · It needs `ANTHROPIC_API_KEY` and an engagement id to spend the shared counter against. The graders and the eleven-document golden set were already written and self-test clean; `keepValidProposals` — the half that does not need a model — was exercised directly against all six drop rules. **The prompt itself is ungraded until `yarn eval:primer` runs.**
2026-09-01 · admin · The intake-questions review surface switched from a copy-pack selector to a **kind** selector · Since PORT-11 the kind is what the cartridge branches on: it picks the pack *and* decides which field groups exist. A pack switch could not reach the roster, stage, ask block, claims cluster, documents drop, tools question, the place, or the reach additions, so the screen was reviewing one questionnaire out of seven. Seven views, derived from the kind registry so a new kind cannot be added without appearing there. Found by Taylor.
2026-09-01 · admin · `QuestionStack` passed `kind="portfolio"` hardcoded to steps 1, 4, 7, 8, and 9 · Landed that way across PORT-13/14/15 because the surface had no kind to pass. It does now.
2026-09-01 · PORT-11 amendment · The `other` kind reads the **entity** pack, not `generic` as the kinds scope §2.1 table said · The scope gave `other` a roster and a documents drop — organisation groups — while pointing it at the portfolio pack, so it asked an organisation for "Your projects", offered it "A reel page", and listed "Producers or production companies" as its audiences. `entity` is the neutral organisation pack and is the coherent pairing for the groups the scope itself assigned. One line in `showcase-kinds.ts`; **Taylor's to overrule**, and the scope's table should be corrected either way.
2026-09-01 · PORT-10 · `scripts/eval-primer-live.ts` was missing; the runner named it and it never landed with the service · Written now, plus the `eval:primer:live` script. `eval-primer.ts` also gained an entry guard — it called `main()` on import and exited the process, so its exported `runGoldenSet` could not be imported by anything.
2026-09-01 · PORT-10 / PORT-17 · Both AI services split into a counted and an uncounted half — `readBusinessPrimer`/`proposeFromDocument` and `extractEntries`/`sortDocument` · The run cap is a property of an engagement, not of a model call, and the evals were spending a real client's budget on fixtures: the extraction eval required `EVAL_ENGAGEMENT_ID` and would have burned ten of someone's twenty-five runs per pass. Nothing client-reachable imports the uncounted halves; the actions call the counted ones and only those.
2026-09-01 · PORT-17 · The extraction prompt's shared rules gained a copy-do-not-compose clause after the live eval failed three of ten cases · The model was deriving words rather than copying them: "founder" from "founded", a composed parenthetical title, and "property" where the text said "sanctuary… estate". All three are the embellishment the prompt already forbade in spirit; it now forbids inflection, joining two phrases, and adding a parenthetical, and says a blank field beats a plausible wrong one. Ten of ten pass after the change.

2026-09-02 · ADM-4 commit 2 (PORT-owned file) · `file-drop.tsx` gave every drop the id `file-<fieldKey>` → an entry-keyed drop now appends its entry key · a label finds its input by id and `getElementById` returns the first match, so with three projects all three "Add images" labels opened the FIRST project's picker and the chosen stills uploaded under the first project's `entryKey`. Wrong in the answers document rather than on screen, which is why nobody caught it. Affects the three entry-keyed drops: `project_images`, `piece_images`, `headshot`. Unkeyed drops keep byte-for-byte the id they had. **This is a client-path behaviour change, not a refactor**, which is why it is its own commit and why Taylor ratified it (2026-09-02). Characterized before and after by static render of two `ProjectEntryCard`s: before, label 2 resolved to input 1; after, each label resolves to its own input, and an unkeyed drop still renders `id="file-portrait"`.

2026-09-03 · PORT-18 slice 1 · `SHOWCASE_DISCIPLINES` widened from the v2 doc's five to eleven; the `music` label narrowed from "Music or performance" to "Music — playing, producing, composing" · Taylor's ask ("modelling, DJing, performing arts, musician, photography — including but not limited to"). Keys are storage and none changed; the six new rows and the narrowed label are `[COPY — pending Taylor]`. `DISCIPLINE_FLAVOURS` stays film-only — a discipline earns a pack by someone writing one, and a key mapped to a pack that does not exist is how the film-shaped-strings bug shipped the first time. `yarn verify:tracks` now asserts film is the only discipline with a pack. The `CLAUDE.md` rule "no DJ / music / festival content" governs tayloraucoin.com's own content, not what a client's intake may offer as a discipline; read that way deliberately rather than dropping the option.
2026-09-03 · PORT-18 slice 1 · The start form reads the discipline list through `showcaseDisciplines()` on the `tracks.ts` seam rather than importing `showcase-steps.ts` directly · The one surface reaching past the seam (noted in `docs/admin/specs/00-build-order.md`); the cartridge verifier proves what the seam serves, and it needed the list.
2026-09-03 · PORT-18 slice 1 · Step 1's fast-way intro moved from a literal in `primer-block.tsx` to the copy pack (`primerIntro`), with a generic floor written for a portfolio and overrides for the practice, entity, venture, and service packs · The literal said "explains the business" and "skip your own career history" to every kind, wrong twice for the one kind whose subject is a person. The service pack keeps the old sentence, which was right for it. PORT-18's ingestion step absorbs the block and this slot when it lands; the words are fixed now because the block is live for portfolio clients today and the slot is where the step will read from anyway.
2026-09-03 · PORT-18 slice 2 · The coded track has **ten** steps: `ingest` leads, "About you" is step 2, everything renumbers · Taylor's ask for a step before "About you" for every coded kind; the README's "nine steps exactly" and D-PORT-9's "nine steps hold" are overridden by it, and the README line is corrected at the source. Every count downstream derives from the registry — the shell, the progress bar, the resume list, the document, and now the welcome screen's number word. The durable track is untouched at nine.
2026-09-03 · PORT-18 slice 2 · The welcome screen's "Nine steps." (v2, verbatim) now reads its number from `stepCountFor` — "Ten steps." for the coded track · Forced by the ask; Taylor ratified the word 2026-09-03. The rest of the sentence is v2. Recorded in the v2 doc's amendments.
2026-09-03 · PORT-18 slice 2 · Engagements in flight before the renumber resume one step earlier than where they left off · `currentStep` stores a number, and step N is now the step that used to be N−1. Nothing is lost — every answer stays under its own key — and the client lands one screen back rather than one screen forward. Not migrated: the number is progress bookkeeping, and the next step reached overwrites it.
2026-09-03 · PORT-18 slice 2 · The step renders statically with no run button · A button that did nothing would be worse than none. The paste (`dump`) and the drop (`ingest_documents`) are live and autosave; the read, its confirmation, the in-progress state, and the completed summary land in slices 3–5.
2026-09-03 · PORT-18 · Files and links are **not** read by the model in this ticket; `PORT-21` is the mandatory follow-up (Taylor, 2026-09-03: "we MUST do the files and links after"), scoped the same day in `PORT-21-files-and-links.md`; it was called PORT-19 for an hour until another thread's PORT-19 and PORT-20 landed · The step's files copy says plainly that only the box is read automatically, so the promise on screen matches the mechanism until PORT-21 changes both together.

2026-09-03 · PORT-19 · Eleven approved v2 strings were **superseded, not paraphrased** — the reel question, "Where does your video live?", "What do you actually want out of this?", "What should they do next?", "If you could only show five", the logo option that said "hate", the pages help line, and the availability question's follow-up · Every one of them is Taylor's own note from his 2026-09-03 read of `/admin/intake/questions`, which outranks the v2 doc by the README's source-precedence rule (the doc is the copy authority *until Taylor changes it*, and this is him changing it). Each superseded string lost its `(v2)` marker and carries `[COPY — pending Taylor]`. The v2 doc's own amendment list should record the eleven.
2026-09-03 · PORT-19 · `reel`, `topFive`, `videoHosts`, `videoHostsOther`, `watchUrl`, and `linkPassword` stay in their step schemas while rendering nowhere · D-PORT-11's law at field scale: a field that is not in its step's schema is silently dropped on the next save, so removing them would strip answers already given the moment a client edited an unrelated field on the same step. They keep their labels (marked "retired question" where the label alone would mislead) and are excluded from the business primer's inventory, which is derived from the schemas and would otherwise propose into a field no screen renders.
2026-09-03 · PORT-19 · A project's videos derive from a legacy `watchUrl` rather than migrating it · `videosOf` in `lib/intake/project-videos.ts` reads a project with no `videos` array and one `watchUrl` as one video, with the project's own entry key as the video's — stable across renders, which a minted key would not be, and step 9's home ticks point at it. **Derived, never written**: nothing saves the migration back, so the answers document is untouched until the client opens the card and edits it. One home for the derivation, read by the card, the shortlist, and the output document alike.
2026-09-03 · PORT-19 · "At most one primary video" is enforced in the component, not the schema · Ticking a second unticks the first, in one write. A validator that refused a second would refuse the **save**, and nothing on this form refuses a save (D-INT-4). No primary at all is an ordinary answer.
2026-09-03 · PORT-19 · The five picked from the project rows is the **one capped control on the coded track**, and the sixth tick is refused with a visible line · Everywhere else a limit is stated and never enforced — the pages checklist deliberately lets a client tick a seventh page, because the thing we most want to hear must stay sayable. Five is different: the cap *is* the question ("if you could only show five"), and a control that silently ignored a sixth tap would read as broken. The line stays until something changes rather than fading.
2026-09-03 · PORT-19 · Step 9 renders a shortlist built from step 5's and step 8's answers, assembled in the step page and passed in · `saveStepAnswers` replaces a step's object wholesale, so a component holding a form over step 9 must never also hold another step's answers — the same reason step 6's person-voice name and step 4's roster are read in the page. Six upload queries and two answer reads run for step 9 only.
2026-09-03 · PORT-19 · Home ticks store ids — a file's row id, a `owner:videoKey` video ref — and the intake document resolves them to filenames and labels at render · Storing the resolved title would let a project renamed on step 5 disagree with a shortlist on step 9. A tick whose target has since been deleted prints as "a file they have since removed" rather than vanishing: dropping it would be the document quietly editing an answer.
2026-09-03 · PORT-19 · `IntakeFileLink` and `linkUploads` gained an `id` · The document has to turn a ticked file id back into a filename. Additive; the durable track's rendering is unchanged and `yarn verify:tracks`'s three durable-document checks still pass.
2026-09-03 · PORT-19 · The tap-card classes are exported from `choice-group.tsx` · Step 9's video cards carry an embed above their label, which a `Choice` cannot hold — its label is a string. Exporting the classes is how that card wears the same clothes exactly instead of approximating them with a second set of literals, and it keeps the selected border's colour in one place (no new hexes).
2026-09-03 · PORT-19 · The "nothing to lead with" flag reads three answers instead of `reel` · A project marked front and centre, a video marked lead-with, or anything shortlisted for the home page. Any one of them answers "what goes first"; the flag fires only when none does.
2026-09-03 · PORT-19 · Retiring "Where does your video live?" left the tools group as the only side of its branch, so `for-kinds.tsx` gained `not()` and the branch reads `not(asks("video"))` · The alternative was an `otherwise` with an empty element opposite it, which would have made the "Every kind" overview tag the empty side as though something were there. **A portfolio and a studio now meet nothing at that point** — widening the tools group to them is a scope decision PORT-19 has no authority to make, and it is Taylor's call.
2026-09-03 · PORT-19 · Note 8 (browser recording + transcription) was scoped out at Taylor's request and shipped as a brief instead · `docs/websites/VOICE-NOTE-TRANSCRIPTION-PRIMER.md`, carrying a kickoff prompt and four open questions. **Claude has no audio input and no speech-to-text API** — the Anthropic Messages API takes text, images, PDFs, and documents and nothing else — so the primer recommends OpenAI `gpt-4o-transcribe` and records it as a second third-party data path needing a privacy-page processor entry. Step 7's voice-note copy and its file drop are untouched by PORT-19.
2026-09-03 · PORT-19 · A stale `.next-agent/types/app/scratch-ingest/` blocked `yarn build:agent` and `npx tsc --noEmit` · Generated types for a route that no longer exists on disk; `tsconfig` includes them. Deleted. Not caused by this ticket and recorded only so the next person who meets it does not go looking for a real error.
2026-09-03 · PORT-20 · The browser recorder stops itself at 30 minutes, with a quiet nudge at 20 · Taylor's answer of 2026-09-03. **This is not a rejection on duration** and the README's "size is the only thing that may reject a file" is intact: the recorder ends a take and keeps every byte of it, while the file drop beside it still accepts a recording of any length. The distinction is recorded here rather than left to a reader's inference.
2026-09-03 · PORT-20 · Transcription is a route handler (`app/api/intake/transcribe/route.ts`), not the server action the spec's placement table named · `maxDuration` is a route-segment config, so a server action inherits the ceiling of the page that called it — an action would have meant stamping a five-minute timeout on the render of all ten steps to cover one call that occasionally takes ninety seconds. Same reasoning that already makes `/api/intake/upload` a route. The transcript *save* stayed a server action, as specified.
2026-09-03 · PORT-20 · `FileDrop`'s issue → PUT → confirm ladder was lifted to `app/websites/intake/_lib/upload-file.ts` and both surfaces call it · The spec made reuse binding and the mechanism the dev's call. `FileDrop`'s behaviour is unchanged to the byte, including that a failed *confirm* is still not surfaced there — turning that silent partial success into a visible failure would be an improvement, and an improvement is still a change to the durable track. The recorder does read the confirm result, and gates deleting its local chunks on it.
2026-09-03 · PORT-20 · `FileDrop` gained an optional `onUploaded(fileId)` callback · So a phone memo dropped on step 7 is written out in the same visit rather than on the client's next one. Optional, and no other call site passes it; every durable-track drop behaves exactly as before.
2026-09-03 · PORT-20 · `lib/legal/version.ts` bumped to `2026-09-03` alongside the new OpenAI processor entry · The privacy page's own docstring makes a substantive edit bump the shared version. **Taylor's to confirm:** the terms text itself did not change, so the terms page's "effective" line now moves for a privacy-only edit. Reverting is a one-line change if he would rather the two documents version separately.
2026-09-03 · PORT-20 · The step-7 card shows a voice note's name twice — once as the drop's "Sent" tile, once as its transcript block's heading · The drop's `existing` tiles are unchanged existing behaviour and the transcript block has to name its file when there are two. Passing `existing={[]}` to that one drop collapses it to one mention and is a one-line change; not taken, because deleting a client's upload receipt is a presentation decision this ticket has no authority to make.
2026-09-03 · PORT-20 · Recording, tab-kill recovery, and re-record were **not** exercised against a real microphone, and nothing was exercised in Safari · The Browser pane blocks microphone access and this environment has no Safari driver. What *was* exercised is recorded in the ticket's verification note: the recovery, failed-upload, delete-gate, transcription-retry, and transcript-edit paths were driven end to end against a scratch Postgres with a killed-tab session written directly into IndexedDB. **The microphone path and Safari remain unverified and are Taylor's to run before this reaches a client.**
2026-09-03 · PORT-18 slice 3 · The ingestion inventory excludes choice-backed, reveal-gated, add-on-gated, and input fields on top of the primer's own exclusions · The primer could tolerate a sentence proposed into a radio-backed field because the client met the proposal and waved it off; the run *writes*, so a sentence in a radio field renders as nothing selected and prints in the document as an answer nobody could have chosen. Taylor ratified the cut. The four sets are hand-written and swept by `staleIngestionKeys()`, reported by `yarn eval:primer --inventory`.
2026-09-03 · PORT-18 slice 3 · Slice 3 absorbed the record type, the merge, and the store that the spec had scheduled for slice 4 · The service needs somewhere to put an outcome and the eval needs the merge to grade it, so splitting them would have meant an untested seam for one slice. Slice 4 is now the machine-filled mark across the steps, the intake document's unconfirmed list, and wiring the action to the store — the write path exists and nothing reaches it yet.
2026-09-03 · PORT-18 slice 3 · `keepValidProposals` and `proposeFromDocument` take a field inventory, defaulting to `PRIMER_FIELDS` · The run shows the model a kind-scoped subset, and a key returned for a question this kind is never asked is dropped by the validator rather than trusted because it exists on some other kind's questionnaire. The primer's own callers pass nothing and behave exactly as before.
2026-09-03 · PORT-18 slice 3 · The extraction prompt's shared rules gained a composed-title rule, a category rule, and a date rule after live runs failed on each · Three real defects, each the copy-do-not-compose law leaking through a specific field: a title assembled for something the text never named, `category: "position"` on a CV that never grouped anything, and `when: "roof finished, first six rooms habitable by autumn"` joined from a sentence carrying a date *and* a status. `yarn eval:extract` is 10 of 10 after the change; the ingestion set is 5 of 5.
2026-09-03 · PORT-18 slice 3 · Three eval ceilings were raised after the first live runs, each because the author's guess was low rather than because the model reached · The two-line dump supports four quoted fields, the venture deck's people slide supports six experience entries, and the CV's freelance line names three works. Every value in all three was grounded. **The rules that never moved are the ones that matter:** every value traceable to the source text, no critical field without a verified quote, and no field outside the kind's inventory. The runner now prints a stage's entries whenever its count is out of range, so the next such call is made from the entries rather than from a number.
2026-09-03 · PORT-18 slice 3 · `videoHosts` was removed from the primer's exclusion map · PORT-19 retired the question, and its key is a checkbox group that was never in the text inventory the drift guard sweeps — so listing it made the guard report a stale key that was correctly absent. `videoHostsOther` stays, being free text.
2026-09-03 · PORT-18 slice 4 · The machine-filled mark is cleared by a value comparison, not by a stored flag · The mark renders while the field's current value still equals what the run wrote, so a client's edit clears it with no event, no second save, and nothing that can fail and leave a stale mark behind. Typing the machine's words back brings it back, which is honest — the value really is the machine's. Compared trimmed on both sides: the schema trims what it stores and a textarea does not, so an exact comparison would drop the mark on a trailing space.
2026-09-03 · PORT-18 slice 4 · `Field` gained an optional `value` prop, passed by `TextAnswer` and `LongAnswer` only · The mark needs the current value and `Field` did not have it. Absent everywhere a `Field` wraps something that is not a step answer — a file drop, a choice group, a repeatable block — and absent means no mark, which is correct for all of them. `ChoiceAnswer` passes none because choice-backed fields are excluded from the ingestion inventory entirely.
2026-09-03 · PORT-18 slice 4 · Entries carry one mark per entry, above the card, rather than one per field inside it · The run writes a whole entry at once and an entry is what a client reads as a unit; a mark on each of a project's eleven fields would be eleven copies of one fact. Rendered at five call sites — the experience block and step 5's four kind-shaped arrays — so the entry cards themselves are untouched and the placement is uniform.
2026-09-03 · PORT-18 slice 4 · `maxDuration = 300` on the coded step route · The ingestion run's tail is minutes and a server action inherits the route segment's ceiling. 300 seconds is the Vercel Pro maximum without Fluid compute and is valid either way; **if Fluid compute is on for this project the ceiling may be raised to 800** — Taylor's to confirm, and one number to change. Every other request on the route finishes in well under a second.
2026-09-03 · PORT-18 slice 4 · The one-shot rule is checked twice — in the action before a run is claimed, and inside the store's transaction · The first is an optimisation so a second press costs no run; the second is the guarantee, because only the transaction holds under concurrency. An action that loses the race reports `already_ran` rather than success, so the surface never claims to have written what it did not.
2026-09-03 · PORT-18 slice 4 · The intake document gained a "Filled in from what they sent, and not yet confirmed" section · The other half of M-PORT-33's bargain: the run writes answers, so the document says which of them the client never touched, grouped by step and dated. Empty by construction for every durable engagement and every coded one that never ran the step — the durable document is byte-identical, re-rendered and checked.
2026-09-03 · PORT-18 slice 5 · The confirmation is a card in the flow, not a modal · The thing it asks about is the paste box directly above it, and a dialog would cover the one piece of evidence the client needs to answer the question. Two actions of deliberately unequal weight: a GhostButton to proceed and a plain text link back, because a second button of equal weight makes the choice look like a coin toss rather than a check.
2026-09-03 · PORT-18 slice 5 · The in-progress overlay has no cancel and no spinner · The run happens server-side, so a cancel would either lie about stopping it or leave the client believing a half-written form is safe to edit; what it offers instead is the true reassurance, that the tab can be closed. One gold hairline breathing on a `ingest-breathe` keyframe in `globals.css` rather than a spinner — motion settles and never pleads, and this is the moment a client is most likely to think something has broken. Reduced motion needs no branch: the sitewide collapse turns the keyframe into the static line it is drawn from.
2026-09-03 · PORT-18 slice 5 · The completed state is server-rendered from the stored record, not held in client state · On success the action returns and the component calls `router.refresh()`; the step page reads the record and renders the summary. So the screen a client sees the instant a run finishes is the same one they see on every later visit, rather than a client-state twin of it that could drift.
2026-09-03 · PORT-18 slice 5 · Every failure line lives in the copy pack's `ingestion.failures`, not in the component · `ExtractionBlock` and the retired `PrimerBlock` keep theirs in the component, so this departs from that precedent deliberately: the build order promises Taylor's copy pass is one file, and this step has more of these lines than any other surface on the track. None flexes between kinds.
2026-09-03 · PORT-18 slice 5 · **`PrimerBlock` is retired and deleted, with its action and its copy slot** · PORT-10's step-2 paste box asked for one document and proposed into free-text fields; step 1 now asks for everything and writes into every step, so it was the same question asked twice. Deleted: `primer-block.tsx`, the `runBusinessPrimer` action, `readBusinessPrimer` (its only caller was that action), and the pack's `primerIntro` slot that slice 1 added for it — the words live on in `ingestion.what`, which was written from them. **Kept:** `businessPrimer` in the schema and the label map, marked `@deprecated` under the `unions` precedent, so an engagement that answered it still prints; `answers.primer` and `StepProposals`, so an engagement that ran the primer before today still meets its suggestions; and `acceptPrimerProposal` / `dismissPrimerProposal`, the two actions those clients still need.
2026-09-03 · PORT-18 slice 5 · A deleted throwaway verification route left a generated route type behind that failed the build · `dev:agent` writes `.next-agent/types/app/<route>/page.ts`, which outlives the source file and is included by `tsconfig.json`. Recorded because it will happen again: **deleting a scratch route means deleting its generated types too.** Caught by the build, not by the type-check that ran before it.
2026-09-03 · PORT-22…27 · Six tickets authored in one session (Vesper, Mason, Reeve) against the guide's three-ticket authoring ceiling · Mitigated as batches 1 and 6 were: one governing UX scope (`../CODED-INTAKE-TASTE-UX-SCOPE.md`, approved by Taylor the same day), M-PORT-35…38 recorded at authoring so every ticket cites the same placement and money calls, and one shared copy table (§13) as the single string authority. The `playwright` dependency in PORT-27 is the one open decision, marked, and the ticket states its no-answer default.
2026-09-03 · README · Locked-scope line "No rating sliders on the taste step — the favourites flow replaced them deliberately (handoff decision 6)" amended · Taylor reversed D-PORT-4 in part (2026-09-03): a seven-stop closeness scale with no default, inside a pick the client chose, weighting a note. Toggle-plus-note, words-not-icons, keyboard floor, and absent-until-first-pick carry. Recorded in the taste scope §14; the README now cites it.
2026-09-03 · README · Non-negotiable "No mechanism can charge the client without Taylor initiating it" restated as "No charge the client did not themselves initiate on hosted Checkout at a published price" · M-PORT-38, on Taylor's brief. The post-intake charges (extra pages, balance, care plan) remain Taylor-initiated; the one client-initiated path is allow-listed to `showcase_animations` and settles through the ancillary path only.
2026-09-03 · PORT-21 · `fflate` replaces the spec's `mammoth`, and reads both Office formats with one code path · A DOCX and a PPTX are both zip archives of XML and the run needs only their text. One small pinned dependency instead of one large one, one reader instead of two. Recorded as M-PORT-36; **the new dependency is Taylor's to ratify.**
2026-09-03 · PORT-21 · The eval caught the PPTX reader labelling slide 10 as "slide 3" · It numbered by position in the sorted list rather than by the number in the filename, so a deck missing a slide would point the client and Taylor at the wrong page of the file they are both holding. Ordering was already right; only the label was wrong.
2026-09-03 · PORT-21 · **The extractor was filling `personKey` with a person's name** · `personKey` holds a roster person's *entry key* and decides which block an experience entry is grouped under; a name matches nobody, so the entry silently stopped being grouped. It was in the derived output schema because the derivation only dropped `entryKey`. Both are machinery rather than answers and both are dropped now. Found by the ingestion eval printing entries when a stage's count went out of range — the printing was added two runs earlier for exactly this kind of judgment.
2026-09-03 · PORT-21 · Three eval ceilings raised again, each because the document supports the count · The venture deck's people slide, the CV's freelance line, and the consultant's one-sheet. Every value was grounded in every case. The hard rules — grounding, no critical field without a verified quote, no field outside the kind's inventory, and now no invented line in a transcription — never moved.
2026-09-03 · PORT-21 · `saveVoiceTranscript` renamed to `saveTranscriptEdit` and widened to the ingestion field keys · It now has two callers — step 7's voice card and step 1's source list — and correcting a machine's reading is the same act whichever produced it. A function called "voice" saving a PDF's reading would be a name that lies. One caller updated.
2026-09-03 · PORT-21 · `FileDrop`'s `onUploaded` hook was already there · PORT-20 added it for the voice-note drop the same day; step 1's document drop is its second caller and needed no change to the mechanism. Noted because the anchor this session expected had been refactored underneath it — **the working tree is shared, and an edit that assumes yesterday's shape will miss.**
2026-09-03 · PORT-21 · Links are fetched at the press, not on blur · A fetch is a real action taken on the client's behalf and belongs to the press they confirmed. Fetching mid-typing would also fetch half-written addresses.
2026-09-03 · PORT-21 · The privacy page names the document and link flows before either can run · Three new paragraphs and one processor line, in the plain register the voice-note disclosure already uses: what goes to Anthropic, what is read on our own server and goes nowhere, what is stored and read by Taylor instead, and that a listed page is fetched by the vendor rather than by us and nothing follows a link on it.
2026-09-03 · PORT-21 · **The run now waits for a file still being read, and names any that did not finish** · A client who attached a deck and pressed straight away got a run that silently excluded it — the worst outcome this step can have, because they watched themselves attach it and there is no second press. `runIngestion` polls for up to 20 seconds and puts anything still unfinished in the record's `notReady`, which the completed summary names one by one rather than counting. Found by auditing the build against the spec rather than by a test; the spec had called for it and the first pass did not do it.
2026-09-03 · PORT-21 · The confirmation lists what the run is about to read · A one-shot action has to show its inputs: what you pasted, how many files have been read, how many links, and — in gold, because it is the line worth reading twice — how many are still being read. That last one is the difference between pressing now and waiting, and without it the wait above would be invisible.
2026-09-03 · PORT-21 · `allSettled` and `isSettled` were written for the confirmation and never used; both deleted · Dead on arrival rather than dead later. The confirmation counts statuses directly from the same rows the list renders, so the manifest and the list cannot disagree.
2026-09-04 · PORT-22 · The primer and ingestion exclusion sets were not touched, against the spec's Placement line · `showcase-primer-fields.ts` already excludes the whole taste step (`EXCLUDED_STEPS`), and the ingestion inventory derives from `PRIMER_FIELDS`, so no taste key — retired, legacy, or new — can reach either. Adding the five would have been a redundant entry that a future reader would mistake for load-bearing. Verified rather than assumed: `yarn eval:primer --inventory` reports no stale key and no taste key in any of the six kinds' inventories.
2026-09-04 · PORT-22 · `scripts/eval-primer.ts`'s `darkOrLight` fixture was left as it is, against the spec's Placement line · The fixture proves "a field outside the inventory is caught", and a taste key is outside the inventory whether or not the question is retired. Pointing it at a live text key would have made it test a different thing and weakened the case it was written for.
2026-09-04 · PORT-22 · `RETIRED_TASTE_KEYS` holds seven keys, not the five the spec named · `favourites` and `notes` joined the retired five. Without them `collectUnanswered` would tell every new coded client that we still need to discuss their "Favourite example sites, best first" on the call — a question the step stopped asking. Same behaviour, same reason: a retired question is not an unanswered one.
2026-09-04 · PORT-22 · `collectFlags` gained a required `asOf: Date` rather than defaulting to `new Date()` · The stale-link flag reads a clock, and a silent default is how a non-deterministic document arrives later without anyone deciding to make one. One call site, inside `renderIntakeMarkdown`, which passes its own `generatedAt` — so a document is a pure function of its inputs, which is what the `--document` diff oracle depends on.
2026-09-04 · PORT-22 · The legacy `notes` map is rendered as lines in the taste branch, fixing a defect this ticket did not create · A `Record<string, string>` reaches `renderValue` as a plain object and prints `[object Object]`. It has never shipped to a client because the gallery has been uncurated since PORT-16, but any engagement that used the stub set between 2026-08-26 and 2026-09-01 holds one. Criterion 4 required the old note readable, so fixing it was in scope rather than adjacent.
2026-09-04 · PORT-22 · `TASTE_PICKS_ASKED` (5) and `STALE_CHECK_DAYS` (90) live in `lib/intake/taste-picks.ts` · The spec named no home for the five. It is read by the document's shortfall flag today and by PORT-23's footer counter and picks-intro copy next, and three literals that must agree is how a form ends up asking for five and counting to four.
2026-09-04 · PORT-22 · The taste schema's docstring now says "Step 6", not "Step 5" · Stale since PORT-18 put the ingestion step first and renumbered the track to ten. Nothing reads it; a comment that misnames the step it guards is how the next session miscounts.
2026-09-04 · PORT-23 · `PickScale`'s `onChange` takes an updater as well as an absolute value · Found in the browser, not by reading: four rapid arrow presses left the scale on 1, because every handler computed from the `value` its own render closed over and React had not re-rendered between them. Holding an arrow key — the natural way to move a slider — would have stuck. This is M-PORT-17's hazard in a different shape, and the fix is the same one: the arithmetic runs inside the state updater. Taps still pass an absolute number, because a tap genuinely is absolute.
2026-09-04 · PORT-23 · Reference entry field ids come from the entry's index, not its entry key · `RepeatableBlock` mints a fresh key on every render while its list is empty, so key-derived ids differed between the server render and the browser and the tree failed to hydrate — labels silently stopped pointing at their inputs. Every other entry card on this track already numbers its fields by index (`f-p-title-${index}`) for exactly this reason; this put the convention back.
2026-09-04 · PORT-23 · The footer count is clamped to the ask, so six picks read `PICKED · 5 OF 5` rather than `6 OF 5` · A count that overshoots its own denominator reads as broken, and this line's whole job is to be a plain fact. Under the ask the true count shows.
2026-09-04 · PORT-23 · The met-the-ask line holds the footer for six seconds before yielding the slot back · The scope says "one save cycle", which is not a measurable unit. Six seconds is the undo window this surface already uses, so the two timings a client can notice on this step are the same number.
2026-09-04 · PORT-23 · A reference entry's pick block carries Edit but no Remove · The entry's own Remove, which `RepeatableBlock` already renders, is what removes a reference. Two Removes a few pixels apart meaning different things is how someone deletes the wrong thing.
2026-09-04 · PORT-23 · The "Sites you've found" help line is a new `[COPY — draft]` string the scope's §13 did not table · §10.1 specified the section's behaviour and the add label but no field help, and a `Field` without one reads as an instruction-free box. Flagged for Taylor's pass with the rest.
2026-09-04 · PORT-23 · Verified against a temporary scratch route (`app/scratch-taste`) with a locally curated set and generated 3024×1964 captures · All six shipped sets are uncurated and no database was reachable in this session, so neither a real engagement nor the admin preview was available. The route, its captures, and the stale `.next-agent/types/app/scratch-taste` it left behind were deleted — the same generated-types artefact PORT-19 recorded for `scratch-ingest`, which fails `build:agent` with a missing-module error that looks nothing like its cause.
2026-09-04 · PORT-23 · The footer count renders in document mode too, and is left that way · The admin review surface mounts `QuestionStack` without a `SaveStateProvider`, so the hook's report is a no-op there and the count is unreachable. Suppressing it would be code for a case that cannot occur; this line exists so the next person who wraps that surface in a provider knows what to expect.
2026-09-04 · PORT-24 · See more stays available on a row that has already been picked · The scope's §6.2 diagram draws the picked state as `PICKED · n/7 · Edit · Remove` with no See more, and building it literally meant a client could not look at a site again once they had reacted to it — while the picks list's Edit sends them back to that very row to reconsider a score, which is a thing you do while looking at the site. The button now renders in both the idle and the picked state. Additive and reversible; flagged for Vesper rather than decided quietly.
2026-09-04 · PORT-24 · Focus moves into the dialog from its own effect, not from the one that installs the key handlers · The overlay's first render returns null while the stage is being measured, so the panel does not exist yet when that effect runs and `panel.current?.focus()` was a no-op. Focus stayed on the page behind: the Tab trap still worked, but the first Tab went outside the dialog and a screen reader was never told it had opened. Found in the browser, not by reading.
2026-09-04 · PORT-24 · The overlay's tab order is Close · Previous · Next · Open the site · the pick block, not the scope's "Close, previous, stage link, next" · The arrows are DOM-adjacent because they wrap together for the narrow layout and are separated by CSS `order` only on wide viewports; the "stage link" the scope imagined between them lives below the stage, not in it. The order that ships follows the reading order in both layouts, which is the property that matters.
2026-09-04 · PORT-24 · A composing note now survives its block being unmounted, by committing the draft rather than discarding it · The scope promised "nothing is lost by pressing an arrow" (§7.3) but `PickBlock` held its draft in local state and wrote only on Save, so paging, closing the overlay, or collapsing a group threw away a half-typed sentence. The block now records unsaved edits and commits them on unmount; Cancel clears the record first, so the deliberate discard still discards. This changes `PickBlock` for the row as well as the overlay, which is correct under the same law.
2026-09-04 · PORT-24 · The 1024px frame/strip swap was verified by dispatching a `resize` event by hand · The browser automation's viewport override changes `window.innerWidth` without emitting `resize` to the page, so the swap appeared not to happen. With the event dispatched, the frame unmounts and the strip renders immediately, and the composing note survives the change. Recorded because the next person to test a resize listener in this harness will meet the same false negative.
2026-09-04 · PORT-24 · Deleting a scratch route leaves TWO stale generated artefacts, not one · `.next-agent/types/app/<route>/` (recorded at PORT-19) and also `.next-agent/types/validator.ts`, which keeps importing the deleted page and fails `tsc` with a missing-module error naming a file nobody wrote. Removing `.next-agent/types` entirely and rebuilding regenerates both.
2026-09-04 · PORT-25 · The hosting platforms were removed from `blocked_domains` after the first live run returned nothing · `blocked_domains` blocks subdomains, and a large share of the sites this search exists to find *are* subdomains of a host — `bryancooperdp.format.com` and `corradoserri.format.com` are in the research library that seeded the feature. Blocking `format.com`, `fabrik.io`, `squarespace.com`, `wix.com`, and `webflow.com` to keep marketing pages out threw away most of the good answers with them. Judging what a page *is* belongs to the instruction, which already does it; the block list now holds only pure aggregators.
2026-09-04 · PORT-25 · `max_uses` was raised from 6 to 14 after the first live run spent its whole budget on round-up articles and returned nothing · The model reported the limit honestly and refused to pad, which is the right behaviour and a useless feature. Deciding whether a site is "dark but warm with oversized type" takes more looking than deciding whether a fact is true. `[PROVISIONAL — measured against one live run, not a set]`; a second run at 14 returned two real practitioner sites, both of which independently appear in Taylor's own research library.
2026-09-04 · PORT-25 · The results list is not a live region; one short sr-only line announces each state instead · A live region wrapping six result cards is read out in full every time a search finishes. The announcement is now "Found 2 sites." and the cards are ordinary content.
2026-09-04 · PORT-25 · `findSites` is split from `searchForStyle` so the search can be run against a fixture without spending an engagement's budget · Exactly the split `sortDocument` has from `extractEntries`, for exactly the same reason, with the same "this is not a bypass" note: nothing client-reachable imports it, and the cap is a property of an engagement rather than of the model call.
2026-09-04 · PORT-25 · A live run returns two to six results, not the three to six the spec's criterion names · The instruction tells the model that fewer is better than padding, and on the verified run it returned the two sites it had actually opened and declined to guess at more. Two real links beat six with four invented ones; the surface renders any count from one up. The criterion's floor is withdrawn rather than met.
2026-09-04 · PORT-25 · Criteria 4 and 5 are unverified: the run counter incrementing, and Add-to-my-sites creating a reference in a browser · Both need a real engagement token and no database was reachable. What is verified: the service claims a run before the model is called (read in code and exercised by the stubbed-transport test), and the reference append is the same updater-form write as `savePick`, which was exercised in a browser at PORT-23. Taylor should exercise both once a database is available.
2026-09-04 · PORT-26 · Implemented but deliberately NOT closed · Every acceptance criterion on this ticket is a money path needing a database and `stripe listen`, and no Stripe credentials were authorised in this session. Marking it Complete on a passing type-check would be the one thing the README's non-negotiables and this repo's standards refuse: an unverified claim about money presented as verified. The surface, the refusals, and the `paid_at` invariant are checked by inspection and in a browser; the settlement paths are not.
2026-09-04 · PORT-26 · The step page gained `searchParams`, read only for the taste step · `?added` scrolls the notice into view and shows a confirming line; `?canceled=1` shows the quiet line. Neither decides whether anything was bought — the bought state is `purchasedExtras`, from the settled basket. A query string a browser can type is not evidence that money moved, which is the same reason the deposit screen has a `PaymentConfirming` state at all.
2026-09-04 · PORT-26 · The runbook gained a section 4 for the one charge a client can start themselves, and its "what must never happen" line was reworded · The old line read "a charge the client did not agree to out loud first", which was written when every charge was Taylor-initiated. It now distinguishes sections 1–3 (Taylor runs something) from section 4 (the client presses a priced button on hosted Checkout).
2026-09-04 · PORT-26 · An unsellable `showcase_animations` row renders the notice with no button and one honest line, rather than a price of $0 · `findSellableProductByKey` returns null until Taylor has run `yarn stripe:catalogue --apply` and `yarn db:seed` for the tier. Until then the notice says motion is not available to add here and points at Taylor, which is true; a button that could not charge would not be.
2026-09-04 · PORT-26 · `MotionNotice` replaces the bare `UpsellQuestions` call on the taste step and wraps it · The bought state is the same three questions under a confirming card, so the questions themselves are unchanged and still gated by `Reveal`'s settled-basket condition. Nothing about the animations questions moved.
2026-09-04 · PORT-27 · The document response is captured by a `response` listener rather than taken from `goto`'s return value · `goto` returns the response only when it resolves, and `waitUntil: "networkidle"` never fires on a page with a carousel, an analytics heartbeat, or a long-poll — which is most marketing sites. Those runs threw, and the framing verdict came back "unknown" for sites whose headers were sitting right there. Listening for the last main-frame navigation response survives both a timeout and a redirect chain, which a URL-matching `waitForResponse` predicate does not.
2026-09-04 · PORT-27 · A page that never goes idle falls back to `domcontentloaded` plus a settle pause instead of failing the run · Whatever loaded is captured and the run says so in a note. Refusing to capture a heavy site would make the tool useless on exactly the sites hardest to shoot by hand.
2026-09-04 · PORT-27 · `name` is taken from `<title>` and marked TODO rather than used as-is · A page title is rarely the person's name — the first live run returned "TIM FLOWER" in block capitals. It is a starting point for an edit, not an answer, and the comment says so.
2026-09-04 · PORT-27 · The three live-site captures used for verification were deleted afterwards · Curation is Taylor's (PORT-16), the sets stay uncurated, and captures with no entry pointing at them are orphans that would still ship in the build. Re-shooting any of them is one command.
2026-09-04 · PORT-26 · The Forge list was exercised at the service level against staging rather than through `stripe listen` · `INTAKE_LINK_KEY` is unset on this machine, so no client token can be minted and the surface cannot be driven end to end. What was exercised instead is every refusal, the settlement, the replay, and the `paid_at` invariant, by calling the same functions the webhook branch calls, against real rows in the staging database, with scratch engagements created and deleted in the same run (0 rows left behind). The webhook's own dispatch is a two-line branch on `charge_kind` above those calls. **Stripe's delivery itself is unexercised.**
2026-09-04 · PORT-26 · Creating the Checkout session is unverified because `showcase_animations` has no minted Stripe price on this tier · `findSellableProductByKey` returns null, so the service refuses — which is the behaviour the unsellable-row branch was built for and which was verified. Minting the price is PORT-3's outstanding action (`yarn stripe:catalogue --apply`, then `yarn db:seed`), not a defect in this slice.
2026-09-04 · PORT-25 · The shared run budget is verified; Add-to-my-sites in a browser is not · `claimRun` increments `extraction_runs` by exactly one per claim, refuses at the cap, and a refused claim spends nothing — seven checks against staging. Driving the surface still needs a token and `INTAKE_LINK_KEY` is unset here.
2026-09-04 · PROGRESS.md · The standing note that migrations 0010 and 0011 are pending Taylor's run is corrected for staging · Both are applied there (`engagements.track`, `intake_files.transcript`, `intake_files.transcript_status`; newest journal entry 2026-09-03). **Production is unchecked and the note says so** — a `db:migrate` on a developer machine resolves to staging, because local borrows staging credentials, so nothing run here says anything about the live tier.
