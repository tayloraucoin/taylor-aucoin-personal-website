# PRE — Pre-work (30 min, Taylor only, before any code)

These are not code tickets. They exist here because two of them block site tickets (PRE-03 blocks every CTA; PRE-01/PRE-02 feed content that lands mid-week). Claude does not execute these — sending email and creating accounts are Taylor's actions.

- **PRE-01** Send testimonial request emails to all 5 recommenders (incl. Dawson Whitfield). Latency starts now — this is the only task where waiting costs the day. Use the template from the sprint doc: ask for (1) permission to publish the existing LinkedIn recommendation with name/title, (2) an optional one-to-two-sentence working-style line; offer to draft it for them.
  - Unblocks: SVC-09, HOME-03 (testimonial placement).
- **PRE-02** Email Chan asking for any current scale figure on Calculate/QxMD (users, installs, "second-largest medical calculator service" type framing). One paragraph, no pressure.
  - Unblocks: CASE-01 (optional scale line).
- **PRE-03** Create the Cal.com booking link. 30-min "Intro call" event type. Grab the URL.
  - **Blocks: SVC-02, SVC-11, HOME-01, QA-01.** The URL gets pasted in ~6 places; store it once in `lib/config.ts` (e.g. `BOOKING_URL`) so it's one constant, not six string literals.
- **PRE-04** Pick the photo. Real face, neutral background, not a logo. If none is loved, use the best existing one — do not spend the morning on a photoshoot. Drop the chosen file in `/public`.
  - Unblocks: SVC-09, HOME-04.
