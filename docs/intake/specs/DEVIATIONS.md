# INT — Deviations (append-only)

One line per intentional divergence from a spec or the UX handoff. Never rewrite history. Format:

```
YYYY-MM-DD · <ticket-id> · <what changed> · <why>
```

2026-08-18 · AUTHORING · All eight INT tickets authored in one session (spec-system ceiling is three per authoring thread) · single-session founder request; mitigated by shared TECH-SCOPE data contract and a Forge review pass over the full set.
2026-08-18 · AUTHORING · Build spec §3's "build Form A first" not followed — Form A (public lead capture) is out of scope entirely · the services page + Cal.com CTA already perform lead capture (UX spec §12); ratified during Vesper's pass.
2026-08-18 · INT-1 · Installs use Yarn 4, not the `npm install` in the ticket's criterion 1 and CLAUDE.md · the repo has `yarn.lock` + `.yarnrc.yml`; npm would have written a second lockfile and resolved a different tree. CLAUDE.md corrected at the source and `packageManager: yarn@4.13.0` pinned in package.json.
2026-08-18 · INT-1 · `intake_files.uploaded_at` column added, absent from TECH-SCOPE §4 · INT-6's issuance-then-confirm ruling needs a pending/confirmed fact; adding it here costs nothing and avoids a second migration mid-form-build.
2026-08-18 · INT-1 · `db/client.ts` exports `getDb()` rather than a top-level `db` singleton as CC does · a module-level client would call `requireEnv("DATABASE_URL")` at import time, breaking `next build` and any script that needs only types. Still one client, created once, lazily.
2026-08-18 · INT-1 · Schema files import `lib/types/intake` by relative path, not the `@/` alias · drizzle-kit bundles the schema outside Next's resolver and does not read tsconfig paths.
2026-08-18 · INT-1 · The setup SQL also `revoke`s table privileges from `anon`/`authenticated` beyond enabling RLS · deny-all was the stated intent; without the revoke an anon client can still probe existence.
2026-08-18 · INT-2 · `findEngagementById` added beyond the ticket's named functions · the CLI's `--status` flag (criterion 4's suggested mechanism) and INT-7's renderer both need id-based reads; documented as tooling-only, never a request path.
2026-08-18 · INT-2 · Runtime criteria 1, 3, 5 not verified — no Supabase project exists yet · ticket left at "Code complete", not Complete, so the INT-3/INT-4 gate stays honest.
2026-08-18 · INT-1 · TECH-SCOPE §8's claim that `lib/env.ts` is "the only `process.env` reader" corrected — it was never true on disk · `lib/config.ts` reads `NEXT_PUBLIC_GA_ID` as a literal (Next inlines only literals into the client bundle) and `middleware.ts` reads `NODE_ENV`. The binding rule is narrower and now says so: no *secret* is read outside `lib/env.ts`.
2026-08-18 · INT-1 · `yarn format` reformatted ~35 pre-existing files unrelated to this slice; reverted, keeping formatting to authored files only · a foundation ticket must not carry an unreviewable repo-wide diff. The repo is not prettier-clean; that is a separate pass to schedule, not slice work.
2026-08-18 · INT-1/INT-2 · Verified against a throwaway local Postgres 15 in the session scratchpad, not Supabase · no hosted project exists yet; the migration, the send-once constraint, the token seam, and reissue are all now proven to work rather than assumed. No hosted database was touched.
2026-08-18 · INT-1 · Setup SQL's revokes and bucket insert wrapped in existence guards · `anon`/`authenticated` and `storage.buckets` are Supabase-only, so the unguarded file could not run against a local database — which is exactly where the schema gets verified. Behaviour on Supabase is unchanged.
2026-08-18 · INT-4 · `components/ui/GradientButton.tsx` (a SCAFFOLDED component) extended with a button rendering mode · the step footer needs real buttons and INT-5's Continue must save before navigating; a second component with copied styling would be two homes for one look. Class strings and visual output are unchanged — flagged per CLAUDE.md's rule about scaffolded components.
2026-08-18 · INT-4 · Analytics and SiteHeader suppressed on `/intake` via a client path gate rather than a route group · see TECHNICAL-DECISIONS M-INT-13. Root layout renders both, and the intake layout cannot unrender them.
2026-08-18 · INT-4 · Component files merged versus the ticket's placement list · `text-field.tsx` holds TextField + TextArea, `choice-group.tsx` holds ChoiceCard + ChoiceGroup, and FooterNav lives inside `step-shell.tsx`. Each pair is one idea; splitting them would be file count without separation.
2026-08-18 · INT-4 · `markStepReached` added to `server/services/engagement.ts` (INT-2's file) · the progress write needs a service to call and the seam owns engagement writes; the action stays thin.
2026-08-18 · INT-4 · Two cross-ticket placeholders shipped: `DepositGatePlaceholder` (INT-3) and the `/done` route (INT-7) · the entry router's state machine is complete now, so both branches need a destination. Each carries a TODO naming its owning ticket and neither pretends to work.
2026-08-18 · INT-4 · `StepScaffold` renders sample controls on every step · temporary, and it is what makes the keyboard path, focus ring, and 16px no-zoom rule checkable before INT-5 copies these primitives into fourteen screens. INT-5/INT-6 delete it.
2026-08-18 · INT-4 · Rendering, keyboard, iOS-zoom, and reduced-motion criteria unverified · the preview tooling in this session is anchored to the Conscious Connections repo and cannot start a server for this one; running a dev server from the shell is prohibited. Manual recipe in PROGRESS.md.
2026-08-18 · INT-3 · Deposit amount renders as solid `--color-c2` display type, not the "gold gradient" the UX spec §3 asks for · the site's as-built stat-number treatment is solid gold and no `bg-clip-text` gradient-text technique exists anywhere in this codebase. The UX spec's own authority header defers to as-built reality on shipped surfaces, and inventing a gradient-text idiom for one number would have been a new pattern the next fourteen screens copy.
2026-08-18 · INT-3 · Stripe API version pinned to `2026-07-29.dahlia` in `server/services/deposit.ts` · left unset, an SDK bump silently changes request and webhook shapes on a money path.
2026-08-18 · INT-3 · `fulfillDeposit` returns a `fulfilled | already_paid | unknown` tri-state rather than void · the webhook route needs to log which happened, and an unknown engagement id must not be retried by Stripe (it is not retryable) nor confused with a replay.
2026-08-18 · INT-3 · The Checkout session carries only `engagement_id` in metadata, never the token · the token is the client's credential and has no business living in a third party's dashboard.
2026-08-18 · INT-3 · Money-path verification used SDK-generated signed payloads against a scratch Postgres, not Stripe test mode · no Agora key exists yet. This proved signature acceptance/rejection and fulfillment idempotency; it did not prove a live Checkout round-trip.
2026-08-18 · PROCESS · Downstream tickets may start against an upstream that is "code complete" rather than Complete; verification and the hosted migration become one pass at the end (Taylor's ruling, 2026-08-18) · the gate as written assumed unverified upstream work is unstable foundation. It is not here: later tickets *import* the primitives rather than copying them, so a visual defect is fixed once at its source, and database logic is verifiable against a scratch Postgres without any hosted project. What genuinely cannot be deferred — schema shape, the token seam, fulfillment idempotency — is verified. What is deferred is pixel-level appearance, live Stripe, real email sends, and applying migrations to Supabase.
2026-08-18 · INT-5 · Per-step TypeScript types are inferred from the Zod schemas (`z.infer`) rather than hand-written in `lib/types/intake.ts` as the ticket's placement implies · a schema plus a matching interface is two homes for one shape. `lib/types/intake.ts` keeps only what does not depend on validators, so the layer order holds.
2026-08-18 · INT-5 · Save state reaches the sticky footer through a small client context (`app/intake/_lib/save-state.tsx`) · the indicator belongs in the footer (UX spec §5.1) but the state lives in the form above it, and `StepShell` is a server component. The alternative was making the shell a client component; this keeps it server-rendered and gives INT-6's five steps the same one-line reporting.
2026-08-18 · INT-5 · Step 1 collects its own copy of the contact fields into `answers.business` rather than writing back to the engagement's columns · the row is Taylor's record of the call and the answers are the client's statement; INT-7 should prefer the client's copy where present. Keeps the write path to a single JSONB merge.
2026-08-18 · INT-5 · `StepScaffold`'s sample controls removed rather than converted to the now-controlled `ChoiceGroup` · steps 1-4 exercise every primitive properly, so the samples would have become a second, diverging copy of how a field is wired. Steps 5-9 show a plain placeholder until INT-6.
2026-08-18 · INT-5 · Steps 5-9 validate through a passthrough schema until INT-6 defines them · without it, saving step 1 would silently drop anything already stored under an undefined step key.
2026-08-18 · INT-6 · Uploads go to storage by plain `fetch`/XHR against a signed URL rather than through `@supabase/supabase-js` in the browser · the client library would need a Supabase key in the bundle, which M-INT-8 forbids. A signed upload URL is a complete single-path credential on its own, so the browser still holds no key.
2026-08-18 · INT-6 · The voice-note input carries no `accept` filter, unlike the photo inputs · on some mobile browsers `accept` hides non-matching files entirely, and an old Android memo the OS will not classify as audio is exactly the file we most want. Photos keep `image/*`, where the risk does not apply.
2026-08-18 · INT-6 · Upload issuance and confirmation share one route (`app/api/intake/upload`) distinguished by body shape · one place where a token is resolved, and one round trip per file either way.
2026-08-18 · INT-6 · Already-delivered files render as named tiles rather than thumbnails · showing a thumbnail would mean minting a signed download URL per file on every page load. Filenames are enough for a client confirming they already sent something; INT-7's document is where files are actually retrieved.
2026-08-18 · INT-6 · `intake_files.step` is left null; the field key carries the grouping · `fieldKey` already identifies both the step and the purpose (`voice_note`, `review_screenshots`), so a step number beside it would be a second, drift-prone home for the same fact.
2026-08-18 · INT-6 · `StepScaffold` deleted · all nine steps are now real.
2026-08-18 · INT-7 · Flags are written as bold text rather than the ⚠️ emoji the build spec §6 sample shows · the document is pasted into an LLM and read in a terminal; the house register carries no emoji. The flag semantics are unchanged.
2026-08-18 · INT-7 · An extra flag beyond the build spec's list: insurance not confirmed · "insured" is a trust claim that cannot be invented (requirements doc §B), and the form's own help line promises we will only say it if it is true.
2026-08-18 · INT-7 · Document dates render in America/Vancouver, not UTC · a document generated at 9pm local was stamped with tomorrow's date, which undermines trust in everything else on the page. Found by verification.
2026-08-18 · INT-7 · Completion fires from the client on arrival at /done, not during render · Next prefetches the last step's Continue target, so a render-time write would finish the intake and email the document while the client was still reading step nine. Same reasoning as M-INT-14.
2026-08-18 · INT-7 · `answer-labels.ts` states field labels a second time (the step components declare their own inline) · the document speaks about a client in Taylor's register and the form speaks to the client in theirs; converging them would cost one of the two voices. Noted in the file.
2026-08-18 · INT-5/INT-7 · The step shape guard now drops only malformed fields instead of rejecting the whole save · a single bad value was costing a client every other answer in that step, which contradicts the no-loss law. Found by verification; field names are logged, values never.
2026-08-18 · INT-8 · The resume-token ciphertext is written at engagement creation (as the Forge audit required), and re-written on reissue · engagements created before migration 0001 have no ciphertext and are skipped by the sweep with a counter, rather than mailed a broken link.
2026-08-18 · INT-8 · Reminder deep-linking applies to reminder 2 only · reminders 1 and 3 go to people who have not started or have stopped entirely, for whom the entry screen is the right landing place.
2026-08-19 · INT-3 · Webhook fulfillment now decides on `payment_status`, not on `checkout.session.completed` alone; `async_payment_succeeded` and `async_payment_failed` are also handled · found via the stripe-best-practices skill (docs.stripe.com/checkout/fulfillment): a delayed-notification payment method completes the session while it is still `unpaid` and settles later — fulfilling on `completed` alone would have marked those deposits paid before the money moved, and never fulfilled the ones that genuinely succeeded. Re-verified through the actual route handler: unpaid-completed does not fulfil, async success does, async failure never marks paid, replay stays idempotent, bad signatures still reject.
2026-08-19 · INT-3 · Deposit priced from a saved Stripe Price, not a per-engagement amount · the UX spec §12 assumption "amounts vary per deal" was mine, never confirmed, and wrong — a standard build is a standard price. `deposit_amount_cents` is no longer a creation input; it is written at fulfillment from the settled session as the record of what this client actually paid, since prices change and the intake document should show the deal as struck. `createEngagementInput` drops the field, the CLI drops `--deposit`, and the pay screen reads the price from Stripe so displayed and charged amounts cannot diverge. No migration: the column was already nullable.
2026-08-19 · INT-3 · Webhooks consolidated to one endpoint at `app/api/webhooks/stripe`, with one file per event type under `_handlers/` · `app/api/stripe` and `app/api/agora/stripe` are removed. The `agora/` nesting was meaningless (the entity is Agora either way) and two endpoints on one account meant two signing secrets to rotate. `AGORA_STRIPE_SECRET_KEY`/`AGORA_STRIPE_WEBHOOK_SECRET` dropped — one account, one key, one secret.
2026-08-19 · INT-3 · Added `stripe_events` (migration 0002) so webhook handlers are at-most-once · Stripe delivers at-least-once; deposit fulfillment was already idempotent by query shape but the new ops notifications are not, and one retry would have been two emails about the same money. The event is claimed before the handler runs and the claim is released if it throws, so a failure is retried rather than remembered as done.
2026-08-19 · INT-3 · Every webhook use-case fully implemented; no placeholder branches remain · deposit settlement notifies on the transition only (not on replays); async failure notifies Taylor and deliberately does not email the client, who will hear from their own bank first; `invoice.finalized` emails Taylor the hosted link, which is the mechanism for sending balance and iteration invoices by text or email.
2026-08-19 · STRIPE · Invoice lines now reference catalogue Prices (`pricing: { price }`) instead of inline `unit_amount_decimal` · Taylor spotted that the invoicing service still did the inline-amount thing that had just been fixed on the deposit. Same reasoning: the number is a fact about the business and belongs in one place. A per-line `description` was added as display-only, because every Price under one Product inherits that Product's name and a client would otherwise see two identical "Website build" lines on one invoice.
2026-08-19 · STRIPE · Catalogue rebuilt from how_we_work.pdf / website_toolkit.pdf; the seeded placeholder prices were wrong · the build is $1,200 + GST split half/half, so deposit and balance are $600 each, not the $1,500 each I had seeded. The documents also carry a catalogue I had not seen: two tiers of change round ($500 / $250), four add-ons, and a $250/month recurring Care Plan. One Product per purchasable thing, since invoices display the product name per line.
2026-08-19 · STRIPE · Catalogue script made declarative, idempotent, and dry-run-first · it now retires any active price a product carries that the catalogue does not declare, because the corrected $600 prices would otherwise have sat alongside the wrong $1,500 ones under different nicknames. Product lookup moved from `products.search` to `products.list`: Search is eventually consistent, so a second run created duplicate products instead of finding the ones it had just made — a real hazard in live mode.
