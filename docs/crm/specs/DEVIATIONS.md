# CRM track — deviations (append-only)

Format: `YYYY-MM-DD · <ticket-id> · <what changed> · <why>`

2026-08-21 · process · Batched builds (2 tickets per pass) instead of one-ticket-per-thread · Taylor's instruction. The rule's purpose is preserved: three-place closure and a written report between tickets.
2026-08-21 · CRM-2 · Middleware matcher stayed site-wide instead of `/admin/:path*` · TECH-SCOPE §3 assumed no middleware existed; one already runs site-wide for the consent regime. Narrowing the matcher would have disabled consent everywhere. Admin session refresh is an early branch on `isAdminPath` that returns before the consent work, so the admin surface still gets no consent cookie and the rest of the site is untouched.
2026-08-21 · CRM-2 · `SUPABASE_ANON_KEY` added to the tier collapse in `resolve-tier-env.ts`, not just `lib/env.ts` · TECH-SCOPE §9 listed it as a plain env var; staging and live are separate Supabase projects with different anon keys, so it has to tier like `SUPABASE_URL` does.
2026-08-21 · CRM-2 · Nav lists unbuilt sections as dimmed labels rather than omitting them · A link to a 404 is worse than a dimmed label, and the shape of the tool is useful information. Each later ticket flips one `ready` flag in `admin-shell.tsx`.
2026-08-21 · CRM-3 · `--crm` is a mode that writes only `crm_leads.csv`, not an addition to the two frozen exports · Writing all three when one was asked for is how a stale audit CSV reaches the Cowork rubric. `--all` is redundant with `--crm` and is reported as ignored rather than silently accepted.
2026-08-21 · CRM-3 · Contract tests added to `test/logic.test.ts` mid-slice · CC's "no tests mid-slice" rule governs the CC repo; leadgen has an existing vitest suite over pure functions and its README documents `yarn test`. The column names are a cross-repo contract whose breakage surfaces in another codebase as "wrong file", so they are asserted rather than assumed.
2026-08-21 · CRM-12 · `nextOpensAt` is a wall-clock label, not a `Date` · Converting a future Vancouver wall-clock back to a UTC instant across a DST boundary is fiddly arithmetic whose only consumer is a string in the empty state. The label plus `nextOpensTomorrow` serves the UI with none of the risk.
2026-08-21 · CRM-12 · Explicit avoid-hour ranges feed display copy only, not the tier · Tiers are decided by what is listed as best/fair, so an unlisted hour (9pm) can never be mistaken for callable. Simpler and fails safe.
2026-08-21 · CRM-4 · Any invalid CSV row rejects the whole file rather than importing the valid remainder · The file is produced by our own export command, so a bad row means something upstream is broken. Importing 1587 of 1588 silently would leave one business permanently missing from the call list with no trace.
2026-08-21 · CRM-4 · `SYNC_OWNED` and `columnOf` exported from the service · So the never-touch law can be asserted in verification rather than trusted, and so later surfaces can tell which fields came from Google rather than from Taylor.
2026-08-21 · CRM-5 · "Overdue" is `nextActionAt < now`, not "before midnight today" · First implemented as before-today, which left a 10am callback still reading as "due today" at 4pm — the lead most likely to be forgotten, buried under this afternoon's upcoming ones. Caught in verification against a real database; UX spec §3.1 ("promised time passed") is the authority.
2026-08-21 · CRM-5 · Callback times are chosen as tokens ("tomorrow_am") and resolved server-side, not computed in the browser · Keeps zone logic in one home, and a callback cannot land an hour off because the queue was opened from another timezone.
2026-08-21 · CRM-5 · The fresh band renders 50 at a time and says so ("showing 50 of 182") · Rendering 1,500 rows is wasteful, and a silent cap reads as "that is all there is".
2026-08-21 · CRM-5 · `/admin` now redirects to the queue rather than showing a placeholder · The queue is the job; the placeholder existed only until it did.
2026-08-21 · process · CRM-8 pulled into pass 5 alongside CRM-6 and CRM-7, exceeding the 2-ticket batch guideline · Correctness dependency: CRM-7's promo checkbox writes "includes a free small round of changes" into a client-facing email, and without CRM-8 the link silently drops the code. Shipping a written promise the system does not keep is worse than a larger pass. CRM-8 is S and touches three files.
2026-08-21 · CRM-6 · The timeline has no stage-change entries · Stage is derived (D-CRM-2) and therefore has no change history of its own; the attempts, emails, and closing ruling *are* the record of how it moved. Adding a stage-event table would have created a second home for something already reconstructible.
2026-08-21 · CRM-6 · `/admin/leads` search includes closed and do-not-call leads · The queue is where the DNC exclusion belongs. When a business Taylor marked do-not-call rings him back, search is where he finds out why; hiding them would mean re-dialing to find out.
2026-08-21 · CRM-7 · A successful intro send also persists the address to `leads.contact_email` · The dialog lets Taylor type an address for a lead that had none; an address good enough to email is the one to keep, so the next send prefills and the list can show the lead has one.
2026-08-21 · CRM-7 · `promoIncluded` is recorded from the sent body, not the checkbox · Taylor can edit the promo line or its link out of the draft; the stored flag has to describe the message that actually went out.
2026-08-21 · CRM-10 · The intake document is rendered on view, never stored · Answers move while a client works and the signed file links expire after a fortnight; a cached copy would be a second, stale home for what they said. A render failure degrades to a message rather than taking the page down, because the money and the reminder switch are the other reasons to be there.
2026-08-21 · CRM-9 · The minted intake link is shown once, inline, rather than behind a second click · The plaintext token is never stored (M-INT-6), so a screen that can be dismissed before copying loses the link for good.
2026-08-21 · CRM-9 · `unlinkEngagement` detaches without deleting · The engagement holds a deposit and a questionnaire; a mis-link must never put those at risk.
2026-08-21 · CRM-10 · No "send another reminder" control exists, by construction · The ceiling of three is a database constraint, and an admin button able to raise it would make the promise a matter of restraint rather than structure (ADMIN-HANDOFF, D-CRM-13).
2026-08-21 · CRM-11 · The funnel's short column is "Last 7 days", not "This week" · A Monday-morning scoreboard reading "0 dials this week" is true and useless. A rolling window is honest about the same data and does not reset to near-zero every week. Labelled exactly as what it is.
2026-08-21 · CRM-5 · A `conversation` disposition now schedules a follow-up (+3 business days) instead of leaving `next_action_at` null · Found by CRM-11's health metric during verification. "Send me the info" is the most valuable outcome a first call produces and had no date attached, which put the best leads into the exact defect state D-CRM-4 exists to prevent. An explicit time from the caller still wins.
2026-08-21 · CRM-12 · `Intl.DateTimeFormat` instances hoisted to module scope in `lib/crm/call-windows.ts` (Forge) · Measured: constructing one costs ~49µs, reusing one ~6µs, and they were being constructed per lead per request. Behaviour-preserving; proven by a characterization harness pinning tier, label, DST, and day-bounds values.
2026-08-21 · CRM-5 · `loadQueue` memoizes the call window per niche instead of recomputing it inside the sort comparator (Forge) · O(n log n) recomputations measured at 1141 ms for the 1406-lead audit thread against 2 ms memoized. `now` is fixed per request and the niche vocabulary is eight values, so the result is identical.
2026-08-21 · CRM-5 · Number-key disposition accelerators now ignore modifier chords, in-flight saves, and open follow-up prompts (Forge) · Cmd+1 switches browser tabs and would also have logged a call; the keyboard path had no `pending` guard while the buttons did, so one dial could be logged twice and corrupt the scoreboard's denominator.
2026-08-21 · CRM-5 · The trade filter navigates with the router instead of `window.location.href` (Forge) · It forced a full document reload on every filter change while the tabs beside it were client links.
2026-08-21 · CRM-10 · Engagements list totals come from one grouped aggregate (`loadMoneyTotals`) instead of one query per row (Forge) · Behaviour proven identical against the per-row path across paid, unpaid, multi-quantity and empty engagements. `loadEngagementMoney` narrowed to module-local, its last external caller removed.
2026-08-21 · CRM-2 · `ADMIN_EMAILS` retired; admin permission is now a `role` in Supabase `app_metadata` (M-CRM-7, ruled by Taylor) · The allowlist was ratified before anyone tried to create the user. Keeping both would have left two sources of truth for permission, which is the one thing the placement law forbids for entitlement.
2026-08-21 · CRM-2 · Correct credentials for an account with no admin role now sign out and return the same failure as a wrong password · Distinguishing them would let anyone enumerate which addresses have accounts on this deployment.
2026-08-21 · CRM-2 · `AdminUser` gained `role`, narrowed against a closed `ADMIN_ROLES` union · Additive, so no call site changed, and it is the field a `public.users` row would populate later without touching consumers (TECH-SCOPE §13).
2026-08-22 · CRM-5 · A conversation tagged "wants info" or "ready for intake" no longer advances the queue; it stops and offers the email or the intake link · Taylor's feedback: pressing "ready for intake" made the lead vanish with nothing to do next. The two most valuable outcomes a call produces were being logged and then silently discarded. The UX spec always said these jump to the email dialog and to engagement creation; CRM-5 shipped the chips without the follow-through.
2026-08-22 · CRM-5 · "Not now" now asks when instead of logging immediately · `follow.kind === "not_now"` was never set anywhere, so the resurface chips were dead code and the most timing-sensitive outcome got the generic 3-business-day cadence. "After the season" and "next week" are the same chip and very different leads.
2026-08-22 · CRM-5 · The fresh band gained a "Show N more" link, driven by `?shown=` · It reported "showing 50 of 181" with no way past 50. The cap exists to keep the first paint quick, not to hide work.
2026-08-22 · CRM-6 · The lead record opens as a right-hand drawer over the list or the queue, keyed on `?lead=<id>` · Looking someone up is an aside from scanning or from a call block; a full navigation lost your place. The page at `/admin/leads/[id]` stays for pasted links and renders the same `LeadRecord`, so the two cannot drift.
2026-08-22 · CRM-7 · The intro email carries the phone number and puts the sign-off on its own line · "Text me" with no number to text is a small broken promise, and this buyer checks. The number comes from `AGORA` in `lib/config.ts` — the same one the sales page publishes.
2026-08-22 · CRM-5 · Added `docs`-quality operating instructions in-product: `lib/crm/sop.ts` rendered by a "How to work the queue" dialog on the queue · Taylor's request. Every disposition and every "what came of it" branch names what the software does next and when the lead returns.
2026-08-22 · CRM-5 · The focus card's subject is a pinned lead, not a row derived from the list · Taylor's report: logging "ready for intake" flashed the next-step panel and then dropped the lead. Cause: `logAttemptAction` revalidates the queue, a logged lead has a next action so the server stops returning it, the selection effect reassigned to another lead, and the focus card's reset effect wiped the panel. `onHold` pins the call being finished until it is released by `onLogged`; keyboard navigation and disposition accelerators are inert while pinned, so a keystroke cannot log the same dial twice.
2026-08-22 · CRM-5 · "Full record" on the focus card opens the drawer rather than navigating to `/admin/leads/[id]` · It was still a plain anchor to the page — the drawer shipped without this link being rewired, which broke the call block exactly the way a navigation does.
2026-08-22 · CRM-6 · The drawer carries an "Open full page" link · Taylor's call: the drawer is right for the aside, and the page is still what you want for a second tab or a pasted link.
2026-08-22 · CRM-5/6/7 · Call-mode UX redesign commissioned by Taylor; current queue/focus-card interaction model is provisional pending it · Brief at `docs/crm/CALL-MODE-BRIEF.md`. UX settles first in a fresh thread (Vesper + Drummer), founder sign-off, then tech scoping. No further interaction work in this thread.
2026-08-22 · process · Phase 5 tickets (CRM-13…16) authored four-in-one-thread, exceeding the three-ticket authoring ceiling · The scoping thread carried the entire v1.2 UX ruling context (D-CRM-23…30, the call sheet edits, the CC Sheet reconnaissance); splitting the batch would have paid the ceiling's quality dividend twice over in lost context. Tickets remain one-per-build-thread.
2026-08-22 · CRM-15 · The markdown renderer carved out into its own ticket (CRM-17) rather than extracted inside CRM-15 · Capacity assessment measured CRM-15 at 985 lines of restructured live interaction code before the parser work; CRM-12/CRM-5 set the precedent for pulling a small precedented dependency out of an L ticket so the expensive thread spends its context on the state machine. Build order Phase 5 sequences CRM-17 ahead of CRM-15.
2026-08-22 · CRM-13 · The keyboard-inertness guard covers focus-card.tsx's digit handler too, not only call-queue.tsx's j/k · The ticket's Placement section named only call-queue.tsx, but its own criterion 4 requires digits to do nothing "to the queue or focus card." A digit reaching FocusCard's disposition handler behind the overlay is the exact phantom-log defect the ticket exists to prevent. Fixed by threading one `sheetOpen` boolean (read once via useSearchParams in CallQueue) into FocusCard as a prop, guarding both handlers from the same fact.
2026-08-22 · CRM-13 · Vendored Sheet trimmed harder than CC's source · Dropped SheetHeader/SheetFooter/SheetDescription/SheetPanel/SheetTrigger, the `side` prop, and `hideClose` — this repo has exactly one consumer, a fixed right-hand panel whose own text links supply Close and Open-full-page. A smaller file matches "a smaller vendored file is a better pattern to copy" from the ticket's own dev notes; extend when a second use case needs the rest.
2026-08-22 · CRM-13 · No animate plugin added; hand-written CSS keyframes in globals.css, reusing the site's existing --dur-fast/--ease-out motion tokens · Resolves the ticket's own [PROVISIONAL — mechanics] marker. Radix mounts SheetContent already in its open data-state, so a `transition` has no prior frame to animate from; a keyframe `animation` always starts at its own 0% and plays on mount. Reusing --dur-fast/--ease-out rather than picking new literals keeps one motion law instead of two.
2026-08-22 · CRM-13 · Animated exit scoped to Esc/overlay-click/Close button; browser back/forward closes instantly · A local `open` state plus an onAnimationEnd-gated navigation lets Radix's own Presence hold the panel mounted through its exit animation for app-initiated closes. Intercepting `popstate` to animate a back-navigation was rejected — fighting the browser's own history contract for a cosmetic gain is not a trade this surface should make.
2026-08-22 · CRM-13 · No separate reduced-motion rule added in the new component · The sitewide `@media (prefers-reduced-motion: reduce)` rule already in globals.css zeroes every animation-duration, which is spec §5's actual law ("panel transitions collapse") more precisely than this ticket's own Outcome paragraph said ("collapses to a fade") — implemented to match the binding spec text, not the ticket's imprecise restatement of it.
2026-08-22 · CRM-13 · z-index set to overlay z-40 / content z-[41], not CC's z-50/z-[70] · Chosen to sit just above the drawer's prior z-40 and stay below intro-email-dialog.tsx's existing z-50 modal, preserving today's relative stacking order rather than inventing a new scale.
2026-08-22 · CRM-13 · `npm install` was run once by mistake in this Yarn-managed repo (packageManager: yarn@4.13.0), creating a stray package-lock.json and rewriting yarn.lock via npm's resolver · Caught immediately before any commit; package-lock.json deleted and `yarn install` re-run to restore yarn.lock and node_modules to Yarn's own resolution. No lasting effect, logged so the moment isn't a mystery to a future `git diff`.
2026-08-22 · CRM-17 · Criterion 1's claim that the SOP dialog's "bullet lists now render as lists" does not apply · `lib/crm/sop.ts` contains zero markdown list syntax — verified by grep (`^- ` matches nothing) and by rendering the real document through the new component (0 `<ul>` elements). Its definition-style entries (`**No answer** — nobody picked up...`) are bold-led paragraphs, already rendering correctly before this ticket. The rest of criterion 1 — no regression, no block renders as raw markup — is verified and holds: identical 7 `<h3>` headings, no `**`/`` ` `` leaked.
2026-08-22 · CRM-17 · Headings now render through `inline()`; previously heading text rendered raw · Deliberate extension so a future bold/code span inside a heading renders correctly rather than showing literal asterisks. Verified zero effect on CALL_SOP's 7 existing headings — none contain `**` or `` ` ``.
2026-08-22 · CRM-17 · `#` (single-hash) support added, mapping to `<h2>` · Needed for CALL-SHEET.md's title; `CALL_SOP` has no `# ` line and is unaffected.
2026-08-22 · CRM-17 · Verification method: rendered `Markup` against both real fixture documents via `renderToStaticMarkup` in a throwaway script (deleted after), not the live app · `/admin` requires authenticated Supabase credentials not available in this environment; typecheck, build, and lint were run per the kickoff contract, and this additional step exercises the actual acceptance criteria (list/blockquote/table counts, no leaked markup, empty-string and unsupported-syntax handling) against real content rather than by inspection alone.
2026-08-22 · CRM-14 · `logAttemptAction`'s parameter type was not "unchanged in shape" as the ticket claimed — it had no way to accept an explicit `nextActionAt` or any of the five new fields · The service (`logAttempt`) already had a `nextActionAt: z.date()` slot, but the action only exposed `schedule?: ScheduleToken`, resolved to a Date or `null` with no path for a caller-picked datetime. Extended the action's signature with `nextActionAt?: Date` (explicit wins over `schedule`, per the "explicit beats computed" law already in `logAttempt`'s own doc comment) plus `contactName`/`contactEmail`/`phoneOverride`/`preferredChannel`/`linkTexted`, all passed through.
2026-08-22 · CRM-14 · Extracted `clearableEmail`/`clearablePhone` as shared consts in `lib/validators/crm.ts`, used by both `updateLeadContactInput` (existing) and the new fields on `logAttemptInput` · Both needed byte-identical "empty string clears" validation (criterion 7's contract); duplicating the `z.union([z.email(), z.literal("")])` shape a second time would have been two homes for one rule. Verified `updateLeadContactInput` parses identically post-refactor.
2026-08-22 · CRM-14 · `TimelineEntry`'s attempt variant gained `linkTexted`, and `loadLeadDetail`'s map and `getLeadStage` call now carry it · Beyond the ticket's literal placement list (which named only `getLeadStage`), but `disposition`/`interestTags`/`note` already ride the same variant for the same reason — it's the general-purpose surface for "everything about an attempt." Leaving `linkTexted` out would mean CRM-16 has to reopen this exact type and this exact map to add one field that's already sitting on the row it reads.
2026-08-22 · CRM-14 · Migration generated via `npm run db:generate` (drizzle-kit, no DB credentials needed — diffs against the committed snapshot per drizzle.config.ts's own comment), not hand-authored · Reviewed as Mason: `db/migrations/0007_curved_vampiro.sql` is three additive statements (one enum type, `call_attempts.link_texted boolean not null default false`, `leads.contact_name text` nullable, `leads.preferred_channel contact_channel` nullable) — no rewrite risk, no lock risk at this table's size, correct nullability and enum shape per the ticket's ruling. Approved, not applied — no database credentials are available in this environment to run `db:migrate`.
2026-08-22 · CRM-14 · Criteria 1, 3, and 4 (transactional atomicity, rollback-on-failure, sync-safety re-import) were not run against a real database — no Supabase credentials available in this environment (same gap noted at CRM-13's closure) · What was verified directly, as real executed code via a throwaway script (deleted after): `logAttemptInput`/`updateLeadContactInput` Zod validation against 6 payloads (bare disposition, full conversation payload, empty-string-clears, malformed email rejected, closed-vocabulary preferredChannel rejected, post-refactor updateLeadContactInput) and `getLeadStage`'s `hadTextedLink` branch plus three regression cases (email-only, neither-sent, terminal-outranks). All passed. Criteria 1/3/4 need a follow-up pass with real database access before CRM-15 is built against this contract with full confidence — the code path is inspected and type-correct, not transaction-tested.
2026-08-22 · CRM-15 · D-CRM-24's log-on-click required a write seam that did not exist; added `adjustFollowUp` service + `adjustFollowUpAction` · The old flow logged only *after* a chip was picked, so the chips rode `logAttempt`. Logging on the click inverts that: the attempt row already exists when the chips appear, and routing them back through `logAttempt` would insert a second attempt and inflate the scoreboard's dial denominator. The new seam updates the lead's `nextActionAt`/`nextActionNote`/`closedReason` only, never `call_attempts`. Deliberately not `closeLead`, which would re-stamp `closedAt` to the chip-click time.
2026-08-22 · CRM-15 · Pre-existing defect found and fixed: the "not interested" reason chips wrote to `nextActionNote`, which `logAttempt` discards on every terminal disposition · The terminal branch sets `{closedState, closedAt, nextActionAt: null}` and never writes `nextActionNote`, so every reason Taylor has ever clicked on a "not interested" call was silently dropped. Reasons now go to `closedReason` via the new adjust seam. Same defect class as the dead `not_now` chips logged 2026-08-22 (CRM-5).
2026-08-22 · CRM-15 · Do-not-call is the one disposition that does NOT log on click · D-CRM-24 says non-conversation dispositions log immediately, but the ticket's own non-negotiables also require DNC to keep its one confirm, and §3.2 makes DNC terminal and permanent. Confirm-then-log is the only reading that satisfies both. The phase machine models it explicitly as `confirmingDnc` rather than as a special case inside the click handler.
2026-08-22 · CRM-15 · Interest tags are multi-select, not the single-select chips they were · `call_attempts.interestTags` is an array and always has been; the old UI committed one tag and discarded the possibility of others. A call genuinely can be both "price talk" and "wants info". Next-touch default resolves deterministically: `not_now` present → 1 month, otherwise the cadence's 3 business days.
2026-08-22 · CRM-15 · `QueueLead` gained `contactName` and `preferredChannel` · The conversation form prefills from what a prior call already learned (D-CRM-25's "nothing re-entered"); without these on the projection the form would ask a second time for a name already in the database.
2026-08-22 · CRM-15 · The subject is pinned on entering call mode, not on save · Pinning only at save time left a window where a revalidation could reassign the selection mid-dial and put the next lead's name above a conversation happening with the previous one. `Back`/Esc release the pin because nothing was logged; `Next lead` releases it after a log.
2026-08-22 · CRM-15 · `focus-card.tsx` deleted; its 594 lines became `call-panel.tsx`, `lead-facts.tsx`, `conversation-form.tsx`, `call-sheet-column.tsx`, and `keyboard.ts` · The ticket permitted the rename. The facts block was extracted specifically so the pre-call panel and call mode cannot drift about which business is on screen, and the keyboard guards were extracted because every past defect in this area came from two listeners disagreeing about when a key was safe to act on.
2026-08-22 · CRM-15 · Criterion 11 `[NEEDS VALUE AT BUILD]` resolved: the fs read does NOT survive `next build` tracing unaided · Verified by reading `.next/server/app/admin/(protected)/queue/page.js.nft.json` — the file is absent until `outputFileTracingIncludes` names it. Added `"/admin/queue": ["./docs/crm/CALL-SHEET.md"]` to `next.config.ts`, matching the existing precedent for the PDF fonts, and re-verified the path now appears in the route's trace manifest.
2026-08-22 · CRM-15 · Interactive verification blocked at the auth wall; not performed · `/admin/queue` correctly 307s to `/admin/login` without a session, and creating or entering credentials is out of bounds. Mechanically verified instead: `npx tsc --noEmit`, `npm run lint`, and `npm run build` all clean; `/admin/queue` compiles in dev (1723 modules, no errors) and in the production build (11.1 kB, 133 kB first load); public shared JS unchanged at 102 kB; the call sheet resolves from `process.cwd()` and is traced into the route bundle. **Not verified: every behavioural criterion (1-10).** The Vigil induction list — network kill mid-save, modifier chords, double-Enter, refresh mid-conversation, reduced motion — needs a signed-in pass before CRM-16 builds on this machine.
2026-08-22 · process · Running `npm run build` while the dev server was live corrupted `.next` and produced phantom `Cannot read properties of undefined (reading 'call')` and `Cannot find module './5873.js'` errors on `/admin/queue` · Both processes share the directory. Diagnosed by stopping the server, clearing `.next`, and restarting: the route then compiled in 395ms and 307'd correctly. Worth knowing before someone debugs a build artifact as a code defect.
2026-08-22 · CRM-16 · `IntroEmailForm` extracted; `intro-email-dialog.tsx` is now a shell around it · The ticket's advisory called for exactly this and it earned its place: the draft/promo/re-send-confirm/failure logic now has one home shared by the lead-record modal and call mode's inline panel. Duplicating it would have meant two implementations of the CASL guard and the record-before-send law, drifting the day either was fixed.
2026-08-22 · CRM-16 · `logAttempt` now returns `attemptId`, and `logAttemptAction` returns `{attemptId, nextActionAt}` · Two facts the post-call surfaces need back from the write. `attemptId` lets a texted link mark *that* dial rather than "the most recent attempt", which is a race with two tabs open. `nextActionAt` lets the review screen report the date the database actually committed rather than restating what the caller asked for — criterion 6 requires the committed date, and the action previously discarded it.
2026-08-22 · CRM-16 · A second pre-existing gap closed: `buildIntroEmail` has always accepted `firstName` and `draftIntroAction` never passed it · Every intro draft opened "Hey —" for someone Taylor had just been on the phone with. Now wired to CRM-14's `contactName`. Same class as the CRM-15 finding (reason chips written to a field the service discards): a parameter that exists, is correct, and is never supplied.
2026-08-22 · CRM-16 · `owedFor` returns two flags rather than one winner · The ticket describes stacked actions when both info and intake are owed, but CRM-15's interim version returned a single winner and silently dropped the email when a call was also ready for intake. "Send me the info and let's get started" is one sentence a prospect actually says.
2026-08-22 · CRM-16 · `QueueLead` gained `projectSummary`, computed server-side by `defaultProjectSummary` · The inline intake mint prefills it. The alternative was reimplementing the sentence in a client component, which would be a second home for wording the client reads on the pay screen.
2026-08-22 · CRM-16 · The text affordance renders both `sms:` and Copy, with CSS choosing by breakpoint rather than sniffing the user agent · The breakpoint already decides which device this is; a UA test would be a second, worse answer to the same question. The href uses `?&body=` — iOS and Android disagree about the separator and this is the form both accept (verified by unit-checking the encoding, not by device testing, which I could not do).
2026-08-22 · CRM-16 · Retry after a failed conversation save keeps its *destination*, not just its payload · Added a `retryReview` ref. Without it a conversation that failed once and then succeeded would fall through to the non-conversation chips, skipping the post-call panel and the receipt — the payload would have been resent verbatim while the yes still dead-ended, which is the exact failure the never-lost contract exists to prevent.
2026-08-22 · CRM-16 · Enter advances from the receipt as well as from a logged non-conversation, and never from the post-call panel · Criterion 6 wants Enter on review; criterion 7 forbids Enter sending anything. Making Enter inert on the post-call panel satisfies both — it is the one phase where a stray Return could fire an outbound action the caller opened deliberately.
2026-08-22 · CRM-16 · Interactive verification blocked at the auth wall; not performed · Same limit as CRM-15. `npx tsc --noEmit`, `npm run lint`, and `npm run build` all clean; `/admin/queue` compiles in dev (1730 modules) and in the production build (13 kB, 135 kB first load); public shared JS unchanged at 102 kB. Verified by executing real code in a throwaway script (deleted after): the text and email composition against 10 cases including the deployed-origin and no-localhost checks (criterion 10), promo on/off, name/no-name greetings, trailing-slash normalisation, and the `sms:` href's encoding and round-trip. **Not verified: the whole Vigil induction list** — email-success, email-fail, re-send-confirm, text-mobile, text-desktop, intake-mint, nothing-owed. Those need a signed-in pass with a live Resend key.
2026-08-22 · process · Deleted `.next` while the dev server was still running, which left it serving a missing routes-manifest and 500ing every route · Recovered by stopping the server before clearing and restarting. Companion to the CRM-15 note: the rule is that `.next` may only be cleared while nothing is serving from it.
